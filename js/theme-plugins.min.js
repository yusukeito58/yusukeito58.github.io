(jQuery.easing.jswing = jQuery.easing.swing),
  jQuery.extend(jQuery.easing, {
    def: 'easeOutQuad',
    swing: function(t, e, i, s, n) {
      return jQuery.easing[jQuery.easing.def](t, e, i, s, n);
    },
    easeInQuad: function(t, e, i, s, n) {
      return s * (e /= n) * e + i;
    },
    easeOutQuad: function(t, e, i, s, n) {
      return -s * (e /= n) * (e - 2) + i;
    },
    easeInOutQuad: function(t, e, i, s, n) {
      return (e /= n / 2) < 1
        ? (s / 2) * e * e + i
        : (-s / 2) * (--e * (e - 2) - 1) + i;
    },
    easeInCubic: function(t, e, i, s, n) {
      return s * (e /= n) * e * e + i;
    },
    easeOutCubic: function(t, e, i, s, n) {
      return s * ((e = e / n - 1) * e * e + 1) + i;
    },
    easeInOutCubic: function(t, e, i, s, n) {
      return (e /= n / 2) < 1
        ? (s / 2) * e * e * e + i
        : (s / 2) * ((e -= 2) * e * e + 2) + i;
    },
    easeInQuart: function(t, e, i, s, n) {
      return s * (e /= n) * e * e * e + i;
    },
    easeOutQuart: function(t, e, i, s, n) {
      return -s * ((e = e / n - 1) * e * e * e - 1) + i;
    },
    easeInOutQuart: function(t, e, i, s, n) {
      return (e /= n / 2) < 1
        ? (s / 2) * e * e * e * e + i
        : (-s / 2) * ((e -= 2) * e * e * e - 2) + i;
    },
    easeInQuint: function(t, e, i, s, n) {
      return s * (e /= n) * e * e * e * e + i;
    },
    easeOutQuint: function(t, e, i, s, n) {
      return s * ((e = e / n - 1) * e * e * e * e + 1) + i;
    },
    easeInOutQuint: function(t, e, i, s, n) {
      return (e /= n / 2) < 1
        ? (s / 2) * e * e * e * e * e + i
        : (s / 2) * ((e -= 2) * e * e * e * e + 2) + i;
    },
    easeInSine: function(t, e, i, s, n) {
      return -s * Math.cos((e / n) * (Math.PI / 2)) + s + i;
    },
    easeOutSine: function(t, e, i, s, n) {
      return s * Math.sin((e / n) * (Math.PI / 2)) + i;
    },
    easeInOutSine: function(t, e, i, s, n) {
      return (-s / 2) * (Math.cos((Math.PI * e) / n) - 1) + i;
    },
    easeInExpo: function(t, e, i, s, n) {
      return 0 == e ? i : s * Math.pow(2, 10 * (e / n - 1)) + i;
    },
    easeOutExpo: function(t, e, i, s, n) {
      return e == n ? i + s : s * (-Math.pow(2, (-10 * e) / n) + 1) + i;
    },
    easeInOutExpo: function(t, e, i, s, n) {
      return 0 == e
        ? i
        : e == n
        ? i + s
        : (e /= n / 2) < 1
        ? (s / 2) * Math.pow(2, 10 * (e - 1)) + i
        : (s / 2) * (-Math.pow(2, -10 * --e) + 2) + i;
    },
    easeInCirc: function(t, e, i, s, n) {
      return -s * (Math.sqrt(1 - (e /= n) * e) - 1) + i;
    },
    easeOutCirc: function(t, e, i, s, n) {
      return s * Math.sqrt(1 - (e = e / n - 1) * e) + i;
    },
    easeInOutCirc: function(t, e, i, s, n) {
      return (e /= n / 2) < 1
        ? (-s / 2) * (Math.sqrt(1 - e * e) - 1) + i
        : (s / 2) * (Math.sqrt(1 - (e -= 2) * e) + 1) + i;
    },
    easeInElastic: function(t, e, i, s, n) {
      var o = 1.70158,
        r = 0,
        a = s;
      if (0 == e) return i;

      if (1 == (e /= n)) return i + s;

      if ((r || (r = 0.3 * n), a < Math.abs(s))) {
        a = s;
        var o = r / 4;
      } else var o = (r / (2 * Math.PI)) * Math.asin(s / a);

      return (
        -(
          a *
          Math.pow(2, 10 * (e -= 1)) *
          Math.sin(((e * n - o) * (2 * Math.PI)) / r)
        ) + i
      );
    },
    easeOutElastic: function(t, e, i, s, n) {
      var o = 1.70158,
        r = 0,
        a = s;
      if (0 == e) return i;

      if (1 == (e /= n)) return i + s;

      if ((r || (r = 0.3 * n), a < Math.abs(s))) {
        a = s;
        var o = r / 4;
      } else var o = (r / (2 * Math.PI)) * Math.asin(s / a);

      return (
        a * Math.pow(2, -10 * e) * Math.sin(((e * n - o) * (2 * Math.PI)) / r) +
        s +
        i
      );
    },
    easeInOutElastic: function(t, e, i, s, n) {
      var o = 1.70158,
        r = 0,
        a = s;
      if (0 == e) return i;

      if (2 == (e /= n / 2)) return i + s;

      if ((r || (r = n * (0.3 * 1.5)), a < Math.abs(s))) {
        a = s;
        var o = r / 4;
      } else var o = (r / (2 * Math.PI)) * Math.asin(s / a);

      return 1 > e
        ? -0.5 *
            (a *
              Math.pow(2, 10 * (e -= 1)) *
              Math.sin(((e * n - o) * (2 * Math.PI)) / r)) +
            i
        : a *
            Math.pow(2, -10 * (e -= 1)) *
            Math.sin(((e * n - o) * (2 * Math.PI)) / r) *
            0.5 +
            s +
            i;
    },
    easeInBack: function(t, e, i, s, n, o) {
      return (
        void 0 == o && (o = 1.70158), s * (e /= n) * e * ((o + 1) * e - o) + i
      );
    },
    easeOutBack: function(t, e, i, s, n, o) {
      return (
        void 0 == o && (o = 1.70158),
        s * ((e = e / n - 1) * e * ((o + 1) * e + o) + 1) + i
      );
    },
    easeInOutBack: function(t, e, i, s, n, o) {
      return (
        void 0 == o && (o = 1.70158),
        (e /= n / 2) < 1
          ? (s / 2) * (e * e * (((o *= 1.525) + 1) * e - o)) + i
          : (s / 2) * ((e -= 2) * e * (((o *= 1.525) + 1) * e + o) + 2) + i
      );
    },
    easeInBounce: function(t, e, i, s, n) {
      return s - jQuery.easing.easeOutBounce(t, n - e, 0, s, n) + i;
    },
    easeOutBounce: function(t, e, i, s, n) {
      return (e /= n) < 1 / 2.75
        ? s * (7.5625 * e * e) + i
        : 2 / 2.75 > e
        ? s * (7.5625 * (e -= 1.5 / 2.75) * e + 0.75) + i
        : 2.5 / 2.75 > e
        ? s * (7.5625 * (e -= 2.25 / 2.75) * e + 0.9375) + i
        : s * (7.5625 * (e -= 2.625 / 2.75) * e + 0.984375) + i;
    },
    easeInOutBounce: function(t, e, i, s, n) {
      return n / 2 > e
        ? 0.5 * jQuery.easing.easeInBounce(t, 2 * e, 0, s, n) + i
        : 0.5 * jQuery.easing.easeOutBounce(t, 2 * e - n, 0, s, n) +
            0.5 * s +
            i;
    }
  }),
  (function(t, e) {
    'use strict';
    (t.MixItUp = function() {
      var e = this;
      e._execAction('_constructor', 0),
        t.extend(e, {
          selectors: {
            target: '.mix',
            filter: '.filter',
            sort: '.sort'
          },
          animation: {
            enable: !0,
            effects: 'fade scale',
            duration: 600,
            easing: 'ease',
            perspectiveDistance: '3000',
            perspectiveOrigin: '50% 50%',
            queue: !0,
            queueLimit: 1,
            animateChangeLayout: !1,
            animateResizeContainer: !0,
            animateResizeTargets: !1,
            staggerSequence: !1,
            reverseOut: !1
          },
          callbacks: {
            onMixLoad: !1,
            onMixStart: !1,
            onMixBusy: !1,
            onMixEnd: !1,
            onMixFail: !1,
            _user: !1
          },
          controls: {
            enable: !0,
            live: !1,
            toggleFilterButtons: !1,
            toggleLogic: 'or',
            activeClass: 'active'
          },
          layout: {
            display: 'inline-block',
            containerClass: '',
            containerClassFail: 'fail'
          },
          load: {
            filter: 'all',
            sort: !1
          },
          _$body: null,
          _$container: null,
          _$targets: null,
          _$parent: null,
          _$sortButtons: null,
          _$filterButtons: null,
          _suckMode: !1,
          _mixing: !1,
          _sorting: !1,
          _clicking: !1,
          _loading: !0,
          _changingLayout: !1,
          _changingClass: !1,
          _changingDisplay: !1,
          _origOrder: [],
          _startOrder: [],
          _newOrder: [],
          _activeFilter: null,
          _toggleArray: [],
          _toggleString: '',
          _activeSort: 'default:asc',
          _newSort: null,
          _startHeight: null,
          _newHeight: null,
          _incPadding: !0,
          _newDisplay: null,
          _newClass: null,
          _targetsBound: 0,
          _targetsDone: 0,
          _queue: [],
          _$show: t(),
          _$hide: t()
        }),
        e._execAction('_constructor', 1);
    }),
      (t.MixItUp.prototype = {
        constructor: t.MixItUp,
        _instances: {},
        _handled: {
          _filter: {},
          _sort: {}
        },
        _bound: {
          _filter: {},
          _sort: {}
        },
        _actions: {},
        _filters: {},
        extend: function(e) {
          for (var i in e) t.MixItUp.prototype[i] = e[i];
        },
        addAction: function(e, i, s, n) {
          t.MixItUp.prototype._addHook('_actions', e, i, s, n);
        },
        addFilter: function(e, i, s, n) {
          t.MixItUp.prototype._addHook('_filters', e, i, s, n);
        },
        _addHook: function(e, i, s, n, o) {
          var r = t.MixItUp.prototype[e],
            a = {};
          (o = 1 === o || 'post' === o ? 'post' : 'pre'),
            (a[i] = {}),
            (a[i][o] = {}),
            (a[i][o][s] = n),
            t.extend(!0, r, a);
        },
        _init: function(e, i) {
          var s = this;
          if (
            (s._execAction('_init', 0, arguments),
            i && t.extend(!0, s, i),
            (s._$body = t('body')),
            (s._domNode = e),
            (s._$container = t(e)),
            s._$container.addClass(s.layout.containerClass),
            (s._id = e.id),
            s._platformDetect(),
            (s._brake = s._getPrefixedCSS('transition', 'none')),
            s._refresh(!0),
            (s._$parent = s._$targets.parent().length
              ? s._$targets.parent()
              : s._$container),
            s.load.sort &&
              ((s._newSort = s._parseSort(s.load.sort)),
              (s._newSortString = s.load.sort),
              (s._activeSort = s.load.sort),
              s._sort(),
              s._printSort()),
            (s._activeFilter =
              'all' === s.load.filter
                ? s.selectors.target
                : 'none' === s.load.filter
                ? ''
                : s.load.filter),
            s.controls.enable && s._bindHandlers(),
            s.controls.toggleFilterButtons)
          ) {
            s._buildToggleArray();
            for (var n = 0; n < s._toggleArray.length; n++)
              s._updateControls(
                {
                  filter: s._toggleArray[n],
                  sort: s._activeSort
                },
                !0
              );
          } else
            s.controls.enable &&
              s._updateControls({
                filter: s._activeFilter,
                sort: s._activeSort
              });

          s._filter(),
            (s._init = !0),
            s._$container.data('mixItUp', s),
            s._execAction('_init', 1, arguments),
            s._buildState(),
            s._$targets.css(s._brake),
            s._goMix(s.animation.enable);
        },
        _platformDetect: function() {
          var t = this,
            i = ['Webkit', 'Moz', 'O', 'ms'],
            s = ['webkit', 'moz'],
            n = window.navigator.appVersion.match(/Chrome\/(\d+)\./) || !1,
            o = 'undefined' != typeof InstallTrigger,
            r = function(t) {
              for (var e = 0; e < i.length; e++)
                if (i[e] + 'Transition' in t.style)
                  return {
                    prefix: '-' + i[e].toLowerCase() + '-',
                    vendor: i[e]
                  };

              return 'transition' in t.style ? '' : !1;
            },
            a = r(t._domNode);
          t._execAction('_platformDetect', 0),
            (t._chrome = n ? parseInt(n[1], 10) : !1),
            (t._ff = o
              ? parseInt(window.navigator.userAgent.match(/rv:([^)]+)\)/)[1])
              : !1),
            (t._prefix = a.prefix),
            (t._vendor = a.vendor),
            (t._suckMode = window.atob && t._prefix ? !1 : !0),
            t._suckMode && (t.animation.enable = !1),
            t._ff && t._ff <= 4 && (t.animation.enable = !1);
          for (var l = 0; l < s.length && !window.requestAnimationFrame; l++)
            window.requestAnimationFrame =
              window[s[l] + 'RequestAnimationFrame'];

          'function' != typeof Object.getPrototypeOf &&
            ('object' == typeof 'test'.__proto__
              ? (Object.getPrototypeOf = function(t) {
                  return t.__proto__;
                })
              : (Object.getPrototypeOf = function(t) {
                  return t.constructor.prototype;
                })),
            t._domNode.nextElementSibling === e &&
              Object.defineProperty(Element.prototype, 'nextElementSibling', {
                get: function() {
                  for (var t = this.nextSibling; t; ) {
                    if (1 === t.nodeType) return t;

                    t = t.nextSibling;
                  }
                  return null;
                }
              }),
            t._execAction('_platformDetect', 1);
        },
        _refresh: function(t, i) {
          var s = this;
          s._execAction('_refresh', 0, arguments),
            (s._$targets = s._$container.find(s.selectors.target));
          for (var n = 0; n < s._$targets.length; n++) {
            var o = s._$targets[n];
            if (o.dataset === e || i) {
              o.dataset = {};
              for (var r = 0; r < o.attributes.length; r++) {
                var a = o.attributes[r],
                  l = a.name,
                  h = a.value;
                if (l.indexOf('data-') > -1) {
                  var c = s._helpers._camelCase(l.substring(5, l.length));
                  o.dataset[c] = h;
                }
              }
            }
            o.mixParent === e && (o.mixParent = s._id);
          }
          if (
            (s._$targets.length && t) ||
            (!s._origOrder.length && s._$targets.length)
          ) {
            s._origOrder = [];
            for (var n = 0; n < s._$targets.length; n++) {
              var o = s._$targets[n];
              s._origOrder.push(o);
            }
          }
          s._execAction('_refresh', 1, arguments);
        },
        _bindHandlers: function() {
          var i = this,
            s = t.MixItUp.prototype._bound._filter,
            n = t.MixItUp.prototype._bound._sort;
          i._execAction('_bindHandlers', 0),
            i.controls.live
              ? i._$body
                  .on('click.mixItUp.' + i._id, i.selectors.sort, function() {
                    i._processClick(t(this), 'sort');
                  })
                  .on('click.mixItUp.' + i._id, i.selectors.filter, function() {
                    i._processClick(t(this), 'filter');
                  })
              : ((i._$sortButtons = t(i.selectors.sort)),
                (i._$filterButtons = t(i.selectors.filter)),
                i._$sortButtons.on('click.mixItUp.' + i._id, function() {
                  i._processClick(t(this), 'sort');
                }),
                i._$filterButtons.on('click.mixItUp.' + i._id, function() {
                  i._processClick(t(this), 'filter');
                })),
            (s[i.selectors.filter] =
              s[i.selectors.filter] === e ? 1 : s[i.selectors.filter] + 1),
            (n[i.selectors.sort] =
              n[i.selectors.sort] === e ? 1 : n[i.selectors.sort] + 1),
            i._execAction('_bindHandlers', 1);
        },
        _processClick: function(i, s) {
          var n = this,
            o = function(i, s, o) {
              var r = t.MixItUp.prototype;
              (r._handled['_' + s][n.selectors[s]] =
                r._handled['_' + s][n.selectors[s]] === e
                  ? 1
                  : r._handled['_' + s][n.selectors[s]] + 1),
                r._handled['_' + s][n.selectors[s]] ===
                  r._bound['_' + s][n.selectors[s]] &&
                  (i[(o ? 'remove' : 'add') + 'Class'](n.controls.activeClass),
                  delete r._handled['_' + s][n.selectors[s]]);
            };
          if (
            (n._execAction('_processClick', 0, arguments),
            !n._mixing ||
              (n.animation.queue && n._queue.length < n.animation.queueLimit))
          ) {
            if (((n._clicking = !0), 'sort' === s)) {
              var r = i.attr('data-sort');
              (!i.hasClass(n.controls.activeClass) ||
                r.indexOf('random') > -1) &&
                (t(n.selectors.sort).removeClass(n.controls.activeClass),
                o(i, s),
                n.sort(r));
            }
            if ('filter' === s) {
              var a,
                l = i.attr('data-filter'),
                h = 'or' === n.controls.toggleLogic ? ',' : '';
              n.controls.toggleFilterButtons
                ? (n._buildToggleArray(),
                  i.hasClass(n.controls.activeClass)
                    ? (o(i, s, !0),
                      (a = n._toggleArray.indexOf(l)),
                      n._toggleArray.splice(a, 1))
                    : (o(i, s), n._toggleArray.push(l)),
                  (n._toggleArray = t.grep(n._toggleArray, function(t) {
                    return t;
                  })),
                  (n._toggleString = n._toggleArray.join(h)),
                  n.filter(n._toggleString))
                : i.hasClass(n.controls.activeClass) ||
                  (t(n.selectors.filter).removeClass(n.controls.activeClass),
                  o(i, s),
                  n.filter(l));
            }
            n._execAction('_processClick', 1, arguments);
          } else
            'function' == typeof n.callbacks.onMixBusy &&
              n.callbacks.onMixBusy.call(n._domNode, n._state, n),
              n._execAction('_processClickBusy', 1, arguments);
        },
        _buildToggleArray: function() {
          var t = this,
            e = t._activeFilter.replace(/\s/g, '');
          if (
            (t._execAction('_buildToggleArray', 0, arguments),
            'or' === t.controls.toggleLogic)
          )
            t._toggleArray = e.split(',');
          else {
            (t._toggleArray = e.split('.')),
              !t._toggleArray[0] && t._toggleArray.shift();
            for (var i, s = 0; (i = t._toggleArray[s]); s++)
              t._toggleArray[s] = '.' + i;
          }
          t._execAction('_buildToggleArray', 1, arguments);
        },
        _updateControls: function(i, s) {
          var n = this,
            o = {
              filter: i.filter,
              sort: i.sort
            },
            r = function(t, e) {
              try {
                s && 'filter' === a && 'none' !== o.filter && '' !== o.filter
                  ? t.filter(e).addClass(n.controls.activeClass)
                  : t
                      .removeClass(n.controls.activeClass)
                      .filter(e)
                      .addClass(n.controls.activeClass);
              } catch (i) {}
            },
            a = 'filter',
            l = null;
          n._execAction('_updateControls', 0, arguments),
            i.filter === e && (o.filter = n._activeFilter),
            i.sort === e && (o.sort = n._activeSort),
            o.filter === n.selectors.target && (o.filter = 'all');
          for (var h = 0; 2 > h; h++)
            (l = n.controls.live ? t(n.selectors[a]) : n['_$' + a + 'Buttons']),
              l && r(l, '[data-' + a + '="' + o[a] + '"]'),
              (a = 'sort');

          n._execAction('_updateControls', 1, arguments);
        },
        _filter: function() {
          var e = this;
          e._execAction('_filter', 0);
          for (var i = 0; i < e._$targets.length; i++) {
            var s = t(e._$targets[i]);
            s.is(e._activeFilter)
              ? (e._$show = e._$show.add(s))
              : (e._$hide = e._$hide.add(s));
          }
          e._execAction('_filter', 1);
        },
        _sort: function() {
          var t = this,
            e = function(t) {
              for (var e = t.slice(), i = e.length, s = i; s--; ) {
                var n = parseInt(Math.random() * i),
                  o = e[s];
                (e[s] = e[n]), (e[n] = o);
              }
              return e;
            };
          t._execAction('_sort', 0), (t._startOrder = []);
          for (var i = 0; i < t._$targets.length; i++) {
            var s = t._$targets[i];
            t._startOrder.push(s);
          }
          switch (t._newSort[0].sortBy) {
            case 'default':
              t._newOrder = t._origOrder;
              break;
            case 'random':
              t._newOrder = e(t._startOrder);
              break;
            case 'custom':
              t._newOrder = t._newSort[0].order;
              break;
            default:
              t._newOrder = t._startOrder.concat().sort(function(e, i) {
                return t._compare(e, i);
              });
          }
          t._execAction('_sort', 1);
        },
        _compare: function(t, e, i) {
          i = i ? i : 0;
          var s = this,
            n = s._newSort[i].order,
            o = function(t) {
              return t.dataset[s._newSort[i].sortBy] || 0;
            },
            r = isNaN(1 * o(t)) ? o(t).toLowerCase() : 1 * o(t),
            a = isNaN(1 * o(e)) ? o(e).toLowerCase() : 1 * o(e);
          return a > r
            ? 'asc' === n
              ? -1
              : 1
            : r > a
            ? 'asc' === n
              ? 1
              : -1
            : r === a && s._newSort.length > i + 1
            ? s._compare(t, e, i + 1)
            : 0;
        },
        _printSort: function(t) {
          var e = this,
            i = t ? e._startOrder : e._newOrder,
            s = e._$parent[0].querySelectorAll(e.selectors.target),
            n = s.length ? s[s.length - 1].nextElementSibling : null,
            o = document.createDocumentFragment();
          e._execAction('_printSort', 0, arguments);
          for (var r = 0; r < s.length; r++) {
            var a = s[r],
              l = a.nextSibling;
            'absolute' !== a.style.position &&
              (l && '#text' === l.nodeName && e._$parent[0].removeChild(l),
              e._$parent[0].removeChild(a));
          }
          for (var r = 0; r < i.length; r++) {
            var h = i[r];
            if (
              'default' !== e._newSort[0].sortBy ||
              'desc' !== e._newSort[0].order ||
              t
            )
              o.appendChild(h), o.appendChild(document.createTextNode(' '));
            else {
              var c = o.firstChild;
              o.insertBefore(h, c),
                o.insertBefore(document.createTextNode(' '), h);
            }
          }
          n ? e._$parent[0].insertBefore(o, n) : e._$parent[0].appendChild(o),
            e._execAction('_printSort', 1, arguments);
        },
        _parseSort: function(t) {
          for (
            var e = this,
              i = 'string' == typeof t ? t.split(' ') : [t],
              s = [],
              n = 0;
            n < i.length;
            n++
          ) {
            var o = 'string' == typeof t ? i[n].split(':') : ['custom', i[n]],
              r = {
                sortBy: e._helpers._camelCase(o[0]),
                order: o[1] || 'asc'
              };
            if ((s.push(r), 'default' === r.sortBy || 'random' === r.sortBy))
              break;
          }
          return e._execFilter('_parseSort', s, arguments);
        },
        _parseEffects: function() {
          var t = this,
            e = {
              opacity: '',
              transformIn: '',
              transformOut: '',
              filter: ''
            },
            i = function(e, i, s) {
              if (t.animation.effects.indexOf(e) > -1) {
                if (i) {
                  var n = t.animation.effects.indexOf(e + '(');
                  if (n > -1) {
                    var o = t.animation.effects.substring(n),
                      r = /\(([^)]+)\)/.exec(o),
                      a = r[1];
                    return { val: a };
                  }
                }
                return !0;
              }
              return !1;
            },
            s = function(t, e) {
              return e
                ? '-' === t.charAt(0)
                  ? t.substr(1, t.length)
                  : '-' + t
                : t;
            },
            n = function(t, n) {
              for (
                var o = [
                    ['scale', '.01'],
                    ['translateX', '20px'],
                    ['translateY', '20px'],
                    ['translateZ', '20px'],
                    ['rotateX', '90deg'],
                    ['rotateY', '90deg'],
                    ['rotateZ', '180deg']
                  ],
                  r = 0;
                r < o.length;
                r++
              ) {
                var a = o[r][0],
                  l = o[r][1],
                  h = n && 'scale' !== a;
                e[t] += i(a) ? a + '(' + s(i(a, !0).val || l, h) + ') ' : '';
              }
            };
          return (
            (e.opacity = i('fade') ? i('fade', !0).val || '0' : '1'),
            n('transformIn'),
            t.animation.reverseOut
              ? n('transformOut', !0)
              : (e.transformOut = e.transformIn),
            (e.transition = {}),
            (e.transition = t._getPrefixedCSS(
              'transition',
              'all ' +
                t.animation.duration +
                'ms ' +
                t.animation.easing +
                ', opacity ' +
                t.animation.duration +
                'ms linear'
            )),
            (t.animation.stagger = i('stagger') ? !0 : !1),
            (t.animation.staggerDuration = parseInt(
              i('stagger') && i('stagger', !0).val ? i('stagger', !0).val : 100
            )),
            t._execFilter('_parseEffects', e)
          );
        },
        _buildState: function(t) {
          var e = this,
            i = {};
          return (
            e._execAction('_buildState', 0),
            (i = {
              activeFilter: '' === e._activeFilter ? 'none' : e._activeFilter,
              activeSort:
                t && e._newSortString ? e._newSortString : e._activeSort,
              fail: !e._$show.length && '' !== e._activeFilter,
              $targets: e._$targets,
              $show: e._$show,
              $hide: e._$hide,
              totalTargets: e._$targets.length,
              totalShow: e._$show.length,
              totalHide: e._$hide.length,
              display: t && e._newDisplay ? e._newDisplay : e.layout.display
            }),
            t
              ? e._execFilter('_buildState', i)
              : ((e._state = i), void e._execAction('_buildState', 1))
          );
        },
        _goMix: function(t) {
          var e = this,
            i = function() {
              e._chrome && 31 === e._chrome && o(e._$parent[0]),
                e._setInter(),
                s();
            },
            s = function() {
              var t = window.pageYOffset,
                i = window.pageXOffset;
              document.documentElement.scrollHeight;
              e._getInterMixData(),
                e._setFinal(),
                e._getFinalMixData(),
                window.pageYOffset !== t && window.scrollTo(i, t),
                e._prepTargets(),
                window.requestAnimationFrame
                  ? requestAnimationFrame(n)
                  : setTimeout(function() {
                      n();
                    }, 20);
            },
            n = function() {
              e._animateTargets(), 0 === e._targetsBound && e._cleanUp();
            },
            o = function(t) {
              var e = t.parentElement,
                i = document.createElement('div'),
                s = document.createDocumentFragment();
              e.insertBefore(i, t), s.appendChild(t), e.replaceChild(t, i);
            },
            r = e._buildState(!0);
          e._execAction('_goMix', 0, arguments),
            !e.animation.duration && (t = !1),
            (e._mixing = !0),
            e._$container.removeClass(e.layout.containerClassFail),
            'function' == typeof e.callbacks.onMixStart &&
              e.callbacks.onMixStart.call(e._domNode, e._state, r, e),
            e._$container.trigger('mixStart', [e._state, r, e]),
            e._getOrigMixData(),
            t && !e._suckMode
              ? window.requestAnimationFrame
                ? requestAnimationFrame(i)
                : i()
              : e._cleanUp(),
            e._execAction('_goMix', 1, arguments);
        },
        _getTargetData: function(t, e) {
          var i,
            s = this;
          (t.dataset[e + 'PosX'] = t.offsetLeft),
            (t.dataset[e + 'PosY'] = t.offsetTop),
            s.animation.animateResizeTargets &&
              ((i = s._suckMode
                ? {
                    marginBottom: '',
                    marginRight: ''
                  }
                : window.getComputedStyle(t)),
              (t.dataset[e + 'MarginBottom'] = parseInt(i.marginBottom)),
              (t.dataset[e + 'MarginRight'] = parseInt(i.marginRight)),
              (t.dataset[e + 'Width'] = t.offsetWidth),
              (t.dataset[e + 'Height'] = t.offsetHeight));
        },
        _getOrigMixData: function() {
          var t = this,
            e = t._suckMode
              ? {
                  boxSizing: ''
                }
              : window.getComputedStyle(t._$parent[0]),
            i = e.boxSizing || e[t._vendor + 'BoxSizing'];
          (t._incPadding = 'border-box' === i),
            t._execAction('_getOrigMixData', 0),
            !t._suckMode && (t.effects = t._parseEffects()),
            (t._$toHide = t._$hide.filter(':visible')),
            (t._$toShow = t._$show.filter(':hidden')),
            (t._$pre = t._$targets.filter(':visible')),
            (t._startHeight = t._incPadding
              ? t._$parent.outerHeight()
              : t._$parent.height());
          for (var s = 0; s < t._$pre.length; s++) {
            var n = t._$pre[s];
            t._getTargetData(n, 'orig');
          }
          t._execAction('_getOrigMixData', 1);
        },
        _setInter: function() {
          var t = this;
          t._execAction('_setInter', 0),
            t._changingLayout && t.animation.animateChangeLayout
              ? (t._$toShow.css('display', t._newDisplay),
                t._changingClass &&
                  t._$container
                    .removeClass(t.layout.containerClass)
                    .addClass(t._newClass))
              : t._$toShow.css('display', t.layout.display),
            t._execAction('_setInter', 1);
        },
        _getInterMixData: function() {
          var t = this;
          t._execAction('_getInterMixData', 0);
          for (var e = 0; e < t._$toShow.length; e++) {
            var i = t._$toShow[e];
            t._getTargetData(i, 'inter');
          }
          for (var e = 0; e < t._$pre.length; e++) {
            var i = t._$pre[e];
            t._getTargetData(i, 'inter');
          }
          t._execAction('_getInterMixData', 1);
        },
        _setFinal: function() {
          var t = this;
          t._execAction('_setFinal', 0),
            t._sorting && t._printSort(),
            t._$toHide.removeStyle('display'),
            t._changingLayout &&
              t.animation.animateChangeLayout &&
              t._$pre.css('display', t._newDisplay),
            t._execAction('_setFinal', 1);
        },
        _getFinalMixData: function() {
          var t = this;
          t._execAction('_getFinalMixData', 0);
          for (var e = 0; e < t._$toShow.length; e++) {
            var i = t._$toShow[e];
            t._getTargetData(i, 'final');
          }
          for (var e = 0; e < t._$pre.length; e++) {
            var i = t._$pre[e];
            t._getTargetData(i, 'final');
          }
          (t._newHeight = t._incPadding
            ? t._$parent.outerHeight()
            : t._$parent.height()),
            t._sorting && t._printSort(!0),
            t._$toShow.removeStyle('display'),
            t._$pre.css('display', t.layout.display),
            t._changingClass &&
              t.animation.animateChangeLayout &&
              t._$container
                .removeClass(t._newClass)
                .addClass(t.layout.containerClass),
            t._execAction('_getFinalMixData', 1);
        },
        _prepTargets: function() {
          var e = this,
            i = {
              _in: e._getPrefixedCSS('transform', e.effects.transformIn),
              _out: e._getPrefixedCSS('transform', e.effects.transformOut)
            };
          e._execAction('_prepTargets', 0),
            e.animation.animateResizeContainer &&
              e._$parent.css('height', e._startHeight + 'px');
          for (var s = 0; s < e._$toShow.length; s++) {
            var n = e._$toShow[s],
              o = t(n);
            (n.style.opacity = e.effects.opacity),
              (n.style.display =
                e._changingLayout && e.animation.animateChangeLayout
                  ? e._newDisplay
                  : e.layout.display),
              o.css(i._in),
              e.animation.animateResizeTargets &&
                ((n.style.width = n.dataset.finalWidth + 'px'),
                (n.style.height = n.dataset.finalHeight + 'px'),
                (n.style.marginRight =
                  -(n.dataset.finalWidth - n.dataset.interWidth) +
                  1 * n.dataset.finalMarginRight +
                  'px'),
                (n.style.marginBottom =
                  -(n.dataset.finalHeight - n.dataset.interHeight) +
                  1 * n.dataset.finalMarginBottom +
                  'px'));
          }
          for (var s = 0; s < e._$pre.length; s++) {
            var n = e._$pre[s],
              o = t(n),
              r = {
                x: n.dataset.origPosX - n.dataset.interPosX,
                y: n.dataset.origPosY - n.dataset.interPosY
              },
              i = e._getPrefixedCSS(
                'transform',
                'translate(' + r.x + 'px,' + r.y + 'px)'
              );
            o.css(i),
              e.animation.animateResizeTargets &&
                ((n.style.width = n.dataset.origWidth + 'px'),
                (n.style.height = n.dataset.origHeight + 'px'),
                n.dataset.origWidth - n.dataset.finalWidth &&
                  (n.style.marginRight =
                    -(n.dataset.origWidth - n.dataset.interWidth) +
                    1 * n.dataset.origMarginRight +
                    'px'),
                n.dataset.origHeight - n.dataset.finalHeight &&
                  (n.style.marginBottom =
                    -(n.dataset.origHeight - n.dataset.interHeight) +
                    1 * n.dataset.origMarginBottom +
                    'px'));
          }
          e._execAction('_prepTargets', 1);
        },
        _animateTargets: function() {
          var e = this;
          e._execAction('_animateTargets', 0),
            (e._targetsDone = 0),
            (e._targetsBound = 0),
            e._$parent
              .css(
                e._getPrefixedCSS(
                  'perspective',
                  e.animation.perspectiveDistance + 'px'
                )
              )
              .css(
                e._getPrefixedCSS(
                  'perspective-origin',
                  e.animation.perspectiveOrigin
                )
              ),
            e.animation.animateResizeContainer &&
              e._$parent
                .css(
                  e._getPrefixedCSS(
                    'transition',
                    'height ' + e.animation.duration + 'ms ease'
                  )
                )
                .css('height', e._newHeight + 'px');
          for (var i = 0; i < e._$toShow.length; i++) {
            var s = e._$toShow[i],
              n = t(s),
              o = {
                x: s.dataset.finalPosX - s.dataset.interPosX,
                y: s.dataset.finalPosY - s.dataset.interPosY
              },
              r = e._getDelay(i),
              a = {};
            s.style.opacity = '';
            for (var l = 0; 2 > l; l++) {
              var h = 0 === l ? (h = e._prefix) : '';
              e._ff &&
                e._ff <= 20 &&
                ((a[h + 'transition-property'] = 'all'),
                (a[h + 'transition-timing-function'] =
                  e.animation.easing + 'ms'),
                (a[h + 'transition-duration'] = e.animation.duration + 'ms')),
                (a[h + 'transition-delay'] = r + 'ms'),
                (a[h + 'transform'] = 'translate(' + o.x + 'px,' + o.y + 'px)');
            }
            (e.effects.transform || e.effects.opacity) && e._bindTargetDone(n),
              e._ff && e._ff <= 20
                ? n.css(a)
                : n.css(e.effects.transition).css(a);
          }
          for (var i = 0; i < e._$pre.length; i++) {
            var s = e._$pre[i],
              n = t(s),
              o = {
                x: s.dataset.finalPosX - s.dataset.interPosX,
                y: s.dataset.finalPosY - s.dataset.interPosY
              },
              r = e._getDelay(i);
            (s.dataset.finalPosX !== s.dataset.origPosX ||
              s.dataset.finalPosY !== s.dataset.origPosY) &&
              e._bindTargetDone(n),
              n.css(
                e._getPrefixedCSS(
                  'transition',
                  'all ' +
                    e.animation.duration +
                    'ms ' +
                    e.animation.easing +
                    ' ' +
                    r +
                    'ms'
                )
              ),
              n.css(
                e._getPrefixedCSS(
                  'transform',
                  'translate(' + o.x + 'px,' + o.y + 'px)'
                )
              ),
              e.animation.animateResizeTargets &&
                (s.dataset.origWidth - s.dataset.finalWidth &&
                  1 * s.dataset.finalWidth &&
                  ((s.style.width = s.dataset.finalWidth + 'px'),
                  (s.style.marginRight =
                    -(s.dataset.finalWidth - s.dataset.interWidth) +
                    1 * s.dataset.finalMarginRight +
                    'px')),
                s.dataset.origHeight - s.dataset.finalHeight &&
                  1 * s.dataset.finalHeight &&
                  ((s.style.height = s.dataset.finalHeight + 'px'),
                  (s.style.marginBottom =
                    -(s.dataset.finalHeight - s.dataset.interHeight) +
                    1 * s.dataset.finalMarginBottom +
                    'px')));
          }
          e._changingClass &&
            e._$container
              .removeClass(e.layout.containerClass)
              .addClass(e._newClass);
          for (var i = 0; i < e._$toHide.length; i++) {
            for (
              var s = e._$toHide[i],
                n = t(s),
                r = e._getDelay(i),
                c = {},
                l = 0;
              2 > l;
              l++
            ) {
              var h = 0 === l ? (h = e._prefix) : '';
              (c[h + 'transition-delay'] = r + 'ms'),
                (c[h + 'transform'] = e.effects.transformOut),
                (c.opacity = e.effects.opacity);
            }
            n.css(e.effects.transition).css(c),
              (e.effects.transform || e.effects.opacity) &&
                e._bindTargetDone(n);
          }
          e._execAction('_animateTargets', 1);
        },
        _bindTargetDone: function(e) {
          var i = this,
            s = e[0];
          i._execAction('_bindTargetDone', 0, arguments),
            s.dataset.bound ||
              ((s.dataset.bound = !0),
              i._targetsBound++,
              e.on(
                'webkitTransitionEnd.mixItUp transitionend.mixItUp',
                function(n) {
                  (n.originalEvent.propertyName.indexOf('transform') > -1 ||
                    n.originalEvent.propertyName.indexOf('opacity') > -1) &&
                    t(n.originalEvent.target).is(i.selectors.target) &&
                    (e.off('.mixItUp'),
                    (s.dataset.bound = ''),
                    i._targetDone());
                }
              )),
            i._execAction('_bindTargetDone', 1, arguments);
        },
        _targetDone: function() {
          var t = this;
          t._execAction('_targetDone', 0),
            t._targetsDone++,
            t._targetsDone === t._targetsBound && t._cleanUp(),
            t._execAction('_targetDone', 1);
        },
        _cleanUp: function() {
          var e = this,
            i = e.animation.animateResizeTargets
              ? 'transform opacity width height margin-bottom margin-right'
              : 'transform opacity',
            s = function() {
              e._$targets.removeStyle('transition', e._prefix);
            };
          e._execAction('_cleanUp', 0),
            e._changingLayout
              ? e._$show.css('display', e._newDisplay)
              : e._$show.css('display', e.layout.display),
            e._$targets.css(e._brake),
            e._$targets
              .removeStyle(i, e._prefix)
              .removeAttr(
                'data-inter-pos-x data-inter-pos-y data-final-pos-x data-final-pos-y data-orig-pos-x data-orig-pos-y data-orig-height data-orig-width data-final-height data-final-width data-inter-width data-inter-height data-orig-margin-right data-orig-margin-bottom data-inter-margin-right data-inter-margin-bottom data-final-margin-right data-final-margin-bottom'
              ),
            e._$hide.removeStyle('display'),
            e._$parent.removeStyle(
              'height transition perspective-distance perspective perspective-origin-x perspective-origin-y perspective-origin perspectiveOrigin',
              e._prefix
            ),
            e._sorting &&
              (e._printSort(),
              (e._activeSort = e._newSortString),
              (e._sorting = !1)),
            e._changingLayout &&
              (e._changingDisplay &&
                ((e.layout.display = e._newDisplay), (e._changingDisplay = !1)),
              e._changingClass &&
                (e._$parent
                  .removeClass(e.layout.containerClass)
                  .addClass(e._newClass),
                (e.layout.containerClass = e._newClass),
                (e._changingClass = !1)),
              (e._changingLayout = !1)),
            e._refresh(),
            e._buildState(),
            e._state.fail &&
              e._$container.addClass(e.layout.containerClassFail),
            (e._$show = t()),
            (e._$hide = t()),
            window.requestAnimationFrame && requestAnimationFrame(s),
            (e._mixing = !1),
            'function' == typeof e.callbacks._user &&
              e.callbacks._user.call(e._domNode, e._state, e),
            'function' == typeof e.callbacks.onMixEnd &&
              e.callbacks.onMixEnd.call(e._domNode, e._state, e),
            e._$container.trigger('mixEnd', [e._state, e]),
            e._state.fail &&
              ('function' == typeof e.callbacks.onMixFail &&
                e.callbacks.onMixFail.call(e._domNode, e._state, e),
              e._$container.trigger('mixFail', [e._state, e])),
            e._loading &&
              ('function' == typeof e.callbacks.onMixLoad &&
                e.callbacks.onMixLoad.call(e._domNode, e._state, e),
              e._$container.trigger('mixLoad', [e._state, e])),
            e._queue.length &&
              (e._execAction('_queue', 0),
              e.multiMix(e._queue[0][0], e._queue[0][1], e._queue[0][2]),
              e._queue.splice(0, 1)),
            e._execAction('_cleanUp', 1),
            (e._loading = !1);
        },
        _getPrefixedCSS: function(t, e, i) {
          var s = this,
            n = {},
            o = '',
            r = -1;
          for (r = 0; 2 > r; r++)
            (o = 0 === r ? s._prefix : ''),
              i ? (n[o + t] = o + e) : (n[o + t] = e);

          return s._execFilter('_getPrefixedCSS', n, arguments);
        },
        _getDelay: function(t) {
          var e = this,
            i =
              'function' == typeof e.animation.staggerSequence
                ? e.animation.staggerSequence.call(e._domNode, t, e._state)
                : t,
            s = e.animation.stagger ? i * e.animation.staggerDuration : 0;
          return e._execFilter('_getDelay', s, arguments);
        },
        _parseMultiMixArgs: function(t) {
          for (
            var e = this,
              i = {
                command: null,
                animate: e.animation.enable,
                callback: null
              },
              s = 0;
            s < t.length;
            s++
          ) {
            var n = t[s];
            null !== n &&
              ('object' == typeof n || 'string' == typeof n
                ? (i.command = n)
                : 'boolean' == typeof n
                ? (i.animate = n)
                : 'function' == typeof n && (i.callback = n));
          }
          return e._execFilter('_parseMultiMixArgs', i, arguments);
        },
        _parseInsertArgs: function(e) {
          for (
            var i = this,
              s = {
                index: 0,
                $object: t(),
                multiMix: {
                  filter: i._state.activeFilter
                },
                callback: null
              },
              n = 0;
            n < e.length;
            n++
          ) {
            var o = e[n];
            'number' == typeof o
              ? (s.index = o)
              : 'object' == typeof o && o instanceof t
              ? (s.$object = o)
              : 'object' == typeof o && i._helpers._isElement(o)
              ? (s.$object = t(o))
              : 'object' == typeof o && null !== o
              ? (s.multiMix = o)
              : 'boolean' != typeof o || o
              ? 'function' == typeof o && (s.callback = o)
              : (s.multiMix = !1);
          }
          return i._execFilter('_parseInsertArgs', s, arguments);
        },
        _execAction: function(t, e, i) {
          var s = this,
            n = e ? 'post' : 'pre';
          if (!s._actions.isEmptyObject && s._actions.hasOwnProperty(t))
            for (var o in s._actions[t][n]) s._actions[t][n][o].call(s, i);
        },
        _execFilter: function(t, e, i) {
          var s = this;
          if (s._filters.isEmptyObject || !s._filters.hasOwnProperty(t))
            return e;

          for (var n in s._filters[t]) return s._filters[t][n].call(s, i);
        },
        _helpers: {
          _camelCase: function(t) {
            return t.replace(/-([a-z])/g, function(t) {
              return t[1].toUpperCase();
            });
          },
          _isElement: function(t) {
            return window.HTMLElement
              ? t instanceof HTMLElement
              : null !== t && 1 === t.nodeType && 'string' === t.nodeName;
          }
        },
        isMixing: function() {
          var t = this;
          return t._execFilter('isMixing', t._mixing);
        },
        filter: function() {
          var t = this,
            e = t._parseMultiMixArgs(arguments);
          t._clicking && (t._toggleString = ''),
            t.multiMix(
              {
                filter: e.command
              },
              e.animate,
              e.callback
            );
        },
        sort: function() {
          var t = this,
            e = t._parseMultiMixArgs(arguments);
          t.multiMix(
            {
              sort: e.command
            },
            e.animate,
            e.callback
          );
        },
        changeLayout: function() {
          var t = this,
            e = t._parseMultiMixArgs(arguments);
          t.multiMix(
            {
              changeLayout: e.command
            },
            e.animate,
            e.callback
          );
        },
        multiMix: function() {
          var t = this,
            i = t._parseMultiMixArgs(arguments);
          if ((t._execAction('multiMix', 0, arguments), t._mixing))
            t.animation.queue && t._queue.length < t.animation.queueLimit
              ? (t._queue.push(arguments),
                t.controls.enable &&
                  !t._clicking &&
                  t._updateControls(i.command),
                t._execAction('multiMixQueue', 1, arguments))
              : ('function' == typeof t.callbacks.onMixBusy &&
                  t.callbacks.onMixBusy.call(t._domNode, t._state, t),
                t._$container.trigger('mixBusy', [t._state, t]),
                t._execAction('multiMixBusy', 1, arguments));
          else {
            t.controls.enable &&
              !t._clicking &&
              (t.controls.toggleFilterButtons && t._buildToggleArray(),
              t._updateControls(i.command, t.controls.toggleFilterButtons)),
              t._queue.length < 2 && (t._clicking = !1),
              delete t.callbacks._user,
              i.callback && (t.callbacks._user = i.callback);
            var s = i.command.sort,
              n = i.command.filter,
              o = i.command.changeLayout;
            t._refresh(),
              s &&
                ((t._newSort = t._parseSort(s)),
                (t._newSortString = s),
                (t._sorting = !0),
                t._sort()),
              n !== e &&
                ((n = 'all' === n ? t.selectors.target : n),
                (t._activeFilter = n)),
              t._filter(),
              o &&
                ((t._newDisplay =
                  'string' == typeof o ? o : o.display || t.layout.display),
                (t._newClass = o.containerClass || ''),
                (t._newDisplay !== t.layout.display ||
                  t._newClass !== t.layout.containerClass) &&
                  ((t._changingLayout = !0),
                  (t._changingClass = t._newClass !== t.layout.containerClass),
                  (t._changingDisplay = t._newDisplay !== t.layout.display))),
              t._$targets.css(t._brake),
              t._goMix(
                i.animate ^ t.animation.enable ? i.animate : t.animation.enable
              ),
              t._execAction('multiMix', 1, arguments);
          }
        },
        insert: function() {
          var t = this,
            e = t._parseInsertArgs(arguments),
            i = 'function' == typeof e.callback ? e.callback : null,
            s = document.createDocumentFragment(),
            n = (function() {
              return (
                t._refresh(),
                t._$targets.length
                  ? e.index < t._$targets.length || !t._$targets.length
                    ? t._$targets[e.index]
                    : t._$targets[t._$targets.length - 1].nextElementSibling
                  : t._$parent[0].children[0]
              );
            })();
          if ((t._execAction('insert', 0, arguments), e.$object)) {
            for (var o = 0; o < e.$object.length; o++) {
              var r = e.$object[o];
              s.appendChild(r), s.appendChild(document.createTextNode(' '));
            }
            t._$parent[0].insertBefore(s, n);
          }
          t._execAction('insert', 1, arguments),
            'object' == typeof e.multiMix && t.multiMix(e.multiMix, i);
        },
        prepend: function() {
          var t = this,
            e = t._parseInsertArgs(arguments);
          t.insert(0, e.$object, e.multiMix, e.callback);
        },
        append: function() {
          var t = this,
            e = t._parseInsertArgs(arguments);
          t.insert(t._state.totalTargets, e.$object, e.multiMix, e.callback);
        },
        getOption: function(t) {
          var i = this,
            s = function(t, i) {
              for (
                var s = i.split('.'),
                  n = s.pop(),
                  o = s.length,
                  r = 1,
                  a = s[0] || i;
                (t = t[a]) && o > r;

              )
                (a = s[r]), r++;

              return t !== e ? (t[n] !== e ? t[n] : t) : void 0;
            };
          return t ? i._execFilter('getOption', s(i, t), arguments) : i;
        },
        setOptions: function(e) {
          var i = this;
          i._execAction('setOptions', 0, arguments),
            'object' == typeof e && t.extend(!0, i, e),
            i._execAction('setOptions', 1, arguments);
        },
        getState: function() {
          var t = this;
          return t._execFilter('getState', t._state, t);
        },
        forceRefresh: function() {
          var t = this;
          t._refresh(!1, !0);
        },
        destroy: function(e) {
          var i = this,
            s = t.MixItUp.prototype._bound._filter,
            n = t.MixItUp.prototype._bound._sort;
          i._execAction('destroy', 0, arguments),
            i._$body
              .add(t(i.selectors.sort))
              .add(t(i.selectors.filter))
              .off('.mixItUp');
          for (var o = 0; o < i._$targets.length; o++) {
            var r = i._$targets[o];
            e && (r.style.display = ''), delete r.mixParent;
          }
          i._execAction('destroy', 1, arguments),
            s[i.selectors.filter] && s[i.selectors.filter] > 1
              ? s[i.selectors.filter]--
              : 1 === s[i.selectors.filter] && delete s[i.selectors.filter],
            n[i.selectors.sort] && n[i.selectors.sort] > 1
              ? n[i.selectors.sort]--
              : 1 === n[i.selectors.sort] && delete n[i.selectors.sort],
            delete t.MixItUp.prototype._instances[i._id];
        }
      }),
      (t.fn.mixItUp = function() {
        var i,
          s = arguments,
          n = [],
          o = function(e, i) {
            var s = new t.MixItUp(),
              n = function() {
                return (
                  '00000' + ((16777216 * Math.random()) << 0).toString(16)
                )
                  .substr(-6)
                  .toUpperCase();
              };
            s._execAction('_instantiate', 0, arguments),
              (e.id = e.id ? e.id : 'MixItUp' + n()),
              s._instances[e.id] || ((s._instances[e.id] = s), s._init(e, i)),
              s._execAction('_instantiate', 1, arguments);
          };
        return (
          (i = this.each(function() {
            if (s && 'string' == typeof s[0]) {
              var i = t.MixItUp.prototype._instances[this.id];
              if ('isLoaded' === s[0]) n.push(i ? !0 : !1);
              else {
                var r = i[s[0]](s[1], s[2], s[3]);
                r !== e && n.push(r);
              }
            } else o(this, s[0]);
          })),
          n.length ? (n.length > 1 ? n : n[0]) : i
        );
      }),
      (t.fn.removeStyle = function(i, s) {
        return (
          (s = s ? s : ''),
          this.each(function() {
            for (var n = this, o = i.split(' '), r = 0; r < o.length; r++)
              for (var a = 0; 4 > a; a++) {
                switch (a) {
                  case 0:
                    var l = o[r];
                    break;
                  case 1:
                    var l = t.MixItUp.prototype._helpers._camelCase(l);
                    break;
                  case 2:
                    var l = s + o[r];
                    break;
                  case 3:
                    var l = t.MixItUp.prototype._helpers._camelCase(s + o[r]);
                }
                if (
                  (n.style[l] !== e &&
                    'unknown' != typeof n.style[l] &&
                    n.style[l].length > 0 &&
                    (n.style[l] = ''),
                  !s && 1 === a)
                )
                  break;
              }

            n.attributes &&
              n.attributes.style &&
              n.attributes.style !== e &&
              '' === n.attributes.style.value &&
              n.attributes.removeNamedItem('style');
          })
        );
      });
  })(jQuery),
  (function(t, e, i, s) {
    function n(e, i) {
      (this.settings = null),
        (this.options = t.extend({}, n.Defaults, i)),
        (this.$element = t(e)),
        (this.drag = t.extend({}, g)),
        (this.state = t.extend({}, d)),
        (this.e = t.extend({}, u)),
        (this._plugins = {}),
        (this._supress = {}),
        (this._current = null),
        (this._speed = null),
        (this._coordinates = []),
        (this._breakpoint = null),
        (this._width = null),
        (this._items = []),
        (this._clones = []),
        (this._mergers = []),
        (this._invalidated = {}),
        (this._pipe = []),
        t.each(
          n.Plugins,
          t.proxy(function(t, e) {
            this._plugins[t[0].toLowerCase() + t.slice(1)] = new e(this);
          }, this)
        ),
        t.each(
          n.Pipe,
          t.proxy(function(e, i) {
            this._pipe.push({
              filter: i.filter,
              run: t.proxy(i.run, this)
            });
          }, this)
        ),
        this.setup(),
        this.initialize();
    }
    function o(t) {
      if (t.touches !== s)
        return { x: t.touches[0].pageX, y: t.touches[0].pageY };

      if (t.touches === s) {
        if (t.pageX !== s) return { x: t.pageX, y: t.pageY };

        if (t.pageX === s) return { x: t.clientX, y: t.clientY };
      }
    }
    function r(t) {
      var e,
        s,
        n = i.createElement('div'),
        o = t;
      for (e in o)
        if (((s = o[e]), 'undefined' != typeof n.style[s]))
          return (n = null), [s, e];

      return [!1];
    }
    function a() {
      return r([
        'transition',
        'WebkitTransition',
        'MozTransition',
        'OTransition'
      ])[1];
    }
    function l() {
      return r([
        'transform',
        'WebkitTransform',
        'MozTransform',
        'OTransform',
        'msTransform'
      ])[0];
    }
    function h() {
      return r([
        'perspective',
        'webkitPerspective',
        'MozPerspective',
        'OPerspective',
        'MsPerspective'
      ])[0];
    }
    function c() {
      return 'ontouchstart' in e || !!navigator.msMaxTouchPoints;
    }
    function p() {
      return e.navigator.msPointerEnabled;
    }
    var g, d, u;
    (g = {
      start: 0,
      startX: 0,
      startY: 0,
      current: 0,
      currentX: 0,
      currentY: 0,
      offsetX: 0,
      offsetY: 0,
      distance: null,
      startTime: 0,
      endTime: 0,
      updatedX: 0,
      targetEl: null
    }),
      (d = {
        isTouch: !1,
        isScrolling: !1,
        isSwiping: !1,
        direction: !1,
        inMotion: !1
      }),
      (u = {
        _onDragStart: null,
        _onDragMove: null,
        _onDragEnd: null,
        _transitionEnd: null,
        _resizer: null,
        _responsiveCall: null,
        _goToLoop: null,
        _checkVisibile: null
      }),
      (n.Defaults = {
        items: 3,
        loop: !1,
        center: !1,
        mouseDrag: !0,
        touchDrag: !0,
        pullDrag: !0,
        freeDrag: !1,
        margin: 0,
        stagePadding: 0,
        merge: !1,
        mergeFit: !0,
        autoWidth: !1,
        startPosition: 0,
        rtl: !1,
        smartSpeed: 250,
        fluidSpeed: !1,
        dragEndSpeed: !1,
        responsive: {},
        responsiveRefreshRate: 200,
        responsiveBaseElement: e,
        responsiveClass: !1,
        fallbackEasing: 'swing',
        info: !1,
        nestedItemSelector: !1,
        itemElement: 'div',
        stageElement: 'div',
        themeClass: 'owl-theme',
        baseClass: 'owl-carousel',
        itemClass: 'owl-item',
        centerClass: 'center',
        activeClass: 'active'
      }),
      (n.Width = {
        Default: 'default',
        Inner: 'inner',
        Outer: 'outer'
      }),
      (n.Plugins = {}),
      (n.Pipe = [
        {
          filter: ['width', 'items', 'settings'],
          run: function(t) {
            t.current =
              this._items && this._items[this.relative(this._current)];
          }
        },
        {
          filter: ['items', 'settings'],
          run: function() {
            var t = this._clones,
              e = this.$stage.children('.cloned');
            (e.length !== t.length || (!this.settings.loop && t.length > 0)) &&
              (this.$stage.children('.cloned').remove(), (this._clones = []));
          }
        },
        {
          filter: ['items', 'settings'],
          run: function() {
            var t,
              e,
              i = this._clones,
              s = this._items,
              n = this.settings.loop
                ? i.length - Math.max(2 * this.settings.items, 4)
                : 0;
            for (t = 0, e = Math.abs(n / 2); e > t; t++)
              n > 0
                ? (this.$stage
                    .children()
                    .eq(s.length + i.length - 1)
                    .remove(),
                  i.pop(),
                  this.$stage
                    .children()
                    .eq(0)
                    .remove(),
                  i.pop())
                : (i.push(i.length / 2),
                  this.$stage.append(
                    s[i[i.length - 1]].clone().addClass('cloned')
                  ),
                  i.push(s.length - 1 - (i.length - 1) / 2),
                  this.$stage.prepend(
                    s[i[i.length - 1]].clone().addClass('cloned')
                  ));
          }
        },
        {
          filter: ['width', 'items', 'settings'],
          run: function() {
            var t,
              e,
              i,
              s = this.settings.rtl ? 1 : -1,
              n = (this.width() / this.settings.items).toFixed(3),
              o = 0;
            for (
              this._coordinates = [],
                e = 0,
                i = this._clones.length + this._items.length;
              i > e;
              e++
            )
              (t = this._mergers[this.relative(e)]),
                (t =
                  (this.settings.mergeFit &&
                    Math.min(t, this.settings.items)) ||
                  t),
                (o +=
                  (this.settings.autoWidth
                    ? this._items[this.relative(e)].width() +
                      this.settings.margin
                    : n * t) * s),
                this._coordinates.push(o);
          }
        },
        {
          filter: ['width', 'items', 'settings'],
          run: function() {
            var e,
              i,
              s = (this.width() / this.settings.items).toFixed(3),
              n = {
                width:
                  Math.abs(this._coordinates[this._coordinates.length - 1]) +
                  2 * this.settings.stagePadding,
                'padding-left': this.settings.stagePadding || '',
                'padding-right': this.settings.stagePadding || ''
              };
            if (
              (this.$stage.css(n),
              (n = {
                width: this.settings.autoWidth
                  ? 'auto'
                  : s - this.settings.margin
              }),
              (n[
                this.settings.rtl ? 'margin-left' : 'margin-right'
              ] = this.settings.margin),
              !this.settings.autoWidth &&
                t.grep(this._mergers, function(t) {
                  return t > 1;
                }).length > 0)
            )
              for (e = 0, i = this._coordinates.length; i > e; e++)
                (n.width =
                  Math.abs(this._coordinates[e]) -
                  Math.abs(this._coordinates[e - 1] || 0) -
                  this.settings.margin),
                  this.$stage
                    .children()
                    .eq(e)
                    .css(n);
            else this.$stage.children().css(n);
          }
        },
        {
          filter: ['width', 'items', 'settings'],
          run: function(t) {
            t.current && this.reset(this.$stage.children().index(t.current));
          }
        },
        {
          filter: ['position'],
          run: function() {
            this.animate(this.coordinates(this._current));
          }
        },
        {
          filter: ['width', 'position', 'items', 'settings'],
          run: function() {
            var t,
              e,
              i,
              s,
              n = this.settings.rtl ? 1 : -1,
              o = 2 * this.settings.stagePadding,
              r = this.coordinates(this.current()) + o,
              a = r + this.width() * n,
              l = [];
            for (i = 0, s = this._coordinates.length; s > i; i++)
              (t = this._coordinates[i - 1] || 0),
                (e = Math.abs(this._coordinates[i]) + o * n),
                ((this.op(t, '<=', r) && this.op(t, '>', a)) ||
                  (this.op(e, '<', r) && this.op(e, '>', a))) &&
                  l.push(i);

            this.$stage
              .children('.' + this.settings.activeClass)
              .removeClass(this.settings.activeClass),
              this.$stage
                .children(':eq(' + l.join('), :eq(') + ')')
                .addClass(this.settings.activeClass),
              this.settings.center &&
                (this.$stage
                  .children('.' + this.settings.centerClass)
                  .removeClass(this.settings.centerClass),
                this.$stage
                  .children()
                  .eq(this.current())
                  .addClass(this.settings.centerClass));
          }
        }
      ]),
      (n.prototype.initialize = function() {
        if (
          (this.trigger('initialize'),
          this.$element
            .addClass(this.settings.baseClass)
            .addClass(this.settings.themeClass)
            .toggleClass('owl-rtl', this.settings.rtl),
          this.browserSupport(),
          this.settings.autoWidth && this.state.imagesLoaded !== !0)
        ) {
          var e, i, n;
          if (
            ((e = this.$element.find('img')),
            (i = this.settings.nestedItemSelector
              ? '.' + this.settings.nestedItemSelector
              : s),
            (n = this.$element.children(i).width()),
            e.length && 0 >= n)
          )
            return this.preloadAutoWidthImages(e), !1;
        }
        this.$element.addClass('owl-loading'),
          (this.$stage = t(
            '<' + this.settings.stageElement + ' class="owl-stage"/>'
          ).wrap('<div class="owl-stage-outer">')),
          this.$element.append(this.$stage.parent()),
          this.replace(this.$element.children().not(this.$stage.parent())),
          (this._width = this.$element.width()),
          this.refresh(),
          this.$element.removeClass('owl-loading').addClass('owl-loaded'),
          this.eventsCall(),
          this.internalEvents(),
          this.addTriggerableEvents(),
          this.trigger('initialized');
      }),
      (n.prototype.setup = function() {
        var e = this.viewport(),
          i = this.options.responsive,
          s = -1,
          n = null;
        i
          ? (t.each(i, function(t) {
              e >= t && t > s && (s = Number(t));
            }),
            (n = t.extend({}, this.options, i[s])),
            delete n.responsive,
            n.responsiveClass &&
              this.$element
                .attr('class', function(t, e) {
                  return e.replace(/\b owl-responsive-\S+/g, '');
                })
                .addClass('owl-responsive-' + s))
          : (n = t.extend({}, this.options)),
          (null === this.settings || this._breakpoint !== s) &&
            (this.trigger('change', {
              property: {
                name: 'settings',
                value: n
              }
            }),
            (this._breakpoint = s),
            (this.settings = n),
            this.invalidate('settings'),
            this.trigger('changed', {
              property: {
                name: 'settings',
                value: this.settings
              }
            }));
      }),
      (n.prototype.optionsLogic = function() {
        this.$element.toggleClass('owl-center', this.settings.center),
          this.settings.loop &&
            this._items.length < this.settings.items &&
            (this.settings.loop = !1),
          this.settings.autoWidth &&
            ((this.settings.stagePadding = !1), (this.settings.merge = !1));
      }),
      (n.prototype.prepare = function(e) {
        var i = this.trigger('prepare', { content: e });
        return (
          i.data ||
            (i.data = t('<' + this.settings.itemElement + '/>')
              .addClass(this.settings.itemClass)
              .append(e)),
          this.trigger('prepared', { content: i.data }),
          i.data
        );
      }),
      (n.prototype.update = function() {
        for (
          var e = 0,
            i = this._pipe.length,
            s = t.proxy(function(t) {
              return this[t];
            }, this._invalidated),
            n = {};
          i > e;

        )
          (this._invalidated.all ||
            t.grep(this._pipe[e].filter, s).length > 0) &&
            this._pipe[e].run(n),
            e++;

        this._invalidated = {};
      }),
      (n.prototype.width = function(t) {
        switch ((t = t || n.Width.Default)) {
          case n.Width.Inner:
          case n.Width.Outer:
            return this._width;
          default:
            return (
              this._width -
              2 * this.settings.stagePadding +
              this.settings.margin
            );
        }
      }),
      (n.prototype.refresh = function() {
        if (0 === this._items.length) return !1;

        new Date().getTime();
        this.trigger('refresh'),
          this.setup(),
          this.optionsLogic(),
          this.$stage.addClass('owl-refresh'),
          this.update(),
          this.$stage.removeClass('owl-refresh'),
          (this.state.orientation = e.orientation),
          this.watchVisibility(),
          this.trigger('refreshed');
      }),
      (n.prototype.eventsCall = function() {
        (this.e._onDragStart = t.proxy(function(t) {
          this.onDragStart(t);
        }, this)),
          (this.e._onDragMove = t.proxy(function(t) {
            this.onDragMove(t);
          }, this)),
          (this.e._onDragEnd = t.proxy(function(t) {
            this.onDragEnd(t);
          }, this)),
          (this.e._onResize = t.proxy(function(t) {
            this.onResize(t);
          }, this)),
          (this.e._transitionEnd = t.proxy(function(t) {
            this.transitionEnd(t);
          }, this)),
          (this.e._preventClick = t.proxy(function(t) {
            this.preventClick(t);
          }, this));
      }),
      (n.prototype.onThrottledResize = function() {
        e.clearTimeout(this.resizeTimer),
          (this.resizeTimer = e.setTimeout(
            this.e._onResize,
            this.settings.responsiveRefreshRate
          ));
      }),
      (n.prototype.onResize = function() {
        return this._items.length
          ? this._width === this.$element.width()
            ? !1
            : this.trigger('resize').isDefaultPrevented()
            ? !1
            : ((this._width = this.$element.width()),
              this.invalidate('width'),
              this.refresh(),
              void this.trigger('resized'))
          : !1;
      }),
      (n.prototype.eventsRouter = function(t) {
        var e = t.type;
        'mousedown' === e || 'touchstart' === e
          ? this.onDragStart(t)
          : 'mousemove' === e || 'touchmove' === e
          ? this.onDragMove(t)
          : 'mouseup' === e || 'touchend' === e
          ? this.onDragEnd(t)
          : 'touchcancel' === e && this.onDragEnd(t);
      }),
      (n.prototype.internalEvents = function() {
        var i = (c(), p());
        this.settings.mouseDrag
          ? (this.$stage.on(
              'mousedown',
              t.proxy(function(t) {
                this.eventsRouter(t);
              }, this)
            ),
            this.$stage.on('dragstart', function() {
              return !1;
            }),
            (this.$stage.get(0).onselectstart = function() {
              return !1;
            }))
          : this.$element.addClass('owl-text-select-on'),
          this.settings.touchDrag &&
            !i &&
            this.$stage.on(
              'touchstart touchcancel',
              t.proxy(function(t) {
                this.eventsRouter(t);
              }, this)
            ),
          this.transitionEndVendor &&
            this.on(
              this.$stage.get(0),
              this.transitionEndVendor,
              this.e._transitionEnd,
              !1
            ),
          this.settings.responsive !== !1 &&
            this.on(e, 'resize', t.proxy(this.onThrottledResize, this));
      }),
      (n.prototype.onDragStart = function(s) {
        var n, r, a, l;
        if (
          ((n = s.originalEvent || s || e.event),
          3 === n.which || this.state.isTouch)
        )
          return !1;

        if (
          ('mousedown' === n.type && this.$stage.addClass('owl-grab'),
          this.trigger('drag'),
          (this.drag.startTime = new Date().getTime()),
          this.speed(0),
          (this.state.isTouch = !0),
          (this.state.isScrolling = !1),
          (this.state.isSwiping = !1),
          (this.drag.distance = 0),
          (r = o(n).x),
          (a = o(n).y),
          (this.drag.offsetX = this.$stage.position().left),
          (this.drag.offsetY = this.$stage.position().top),
          this.settings.rtl &&
            (this.drag.offsetX =
              this.$stage.position().left +
              this.$stage.width() -
              this.width() +
              this.settings.margin),
          this.state.inMotion && this.support3d)
        )
          (l = this.getTransformProperty()),
            (this.drag.offsetX = l),
            this.animate(l),
            (this.state.inMotion = !0);
        else if (this.state.inMotion && !this.support3d)
          return (this.state.inMotion = !1), !1;

        (this.drag.startX = r - this.drag.offsetX),
          (this.drag.startY = a - this.drag.offsetY),
          (this.drag.start = r - this.drag.startX),
          (this.drag.targetEl = n.target || n.srcElement),
          (this.drag.updatedX = this.drag.start),
          ('IMG' === this.drag.targetEl.tagName ||
            'A' === this.drag.targetEl.tagName) &&
            (this.drag.targetEl.draggable = !1),
          t(i).on(
            'mousemove.owl.dragEvents mouseup.owl.dragEvents touchmove.owl.dragEvents touchend.owl.dragEvents',
            t.proxy(function(t) {
              this.eventsRouter(t);
            }, this)
          );
      }),
      (n.prototype.onDragMove = function(t) {
        var i, n, r, a, l, h;
        this.state.isTouch &&
          (this.state.isScrolling ||
            ((i = t.originalEvent || t || e.event),
            (n = o(i).x),
            (r = o(i).y),
            (this.drag.currentX = n - this.drag.startX),
            (this.drag.currentY = r - this.drag.startY),
            (this.drag.distance = this.drag.currentX - this.drag.offsetX),
            this.drag.distance < 0
              ? (this.state.direction = this.settings.rtl ? 'right' : 'left')
              : this.drag.distance > 0 &&
                (this.state.direction = this.settings.rtl ? 'left' : 'right'),
            this.settings.loop
              ? this.op(
                  this.drag.currentX,
                  '>',
                  this.coordinates(this.minimum())
                ) && 'right' === this.state.direction
                ? (this.drag.currentX -=
                    (this.settings.center && this.coordinates(0)) -
                    this.coordinates(this._items.length))
                : this.op(
                    this.drag.currentX,
                    '<',
                    this.coordinates(this.maximum())
                  ) &&
                  'left' === this.state.direction &&
                  (this.drag.currentX +=
                    (this.settings.center && this.coordinates(0)) -
                    this.coordinates(this._items.length))
              : ((a = this.settings.rtl
                  ? this.coordinates(this.maximum())
                  : this.coordinates(this.minimum())),
                (l = this.settings.rtl
                  ? this.coordinates(this.minimum())
                  : this.coordinates(this.maximum())),
                (h = this.settings.pullDrag ? this.drag.distance / 5 : 0),
                (this.drag.currentX = Math.max(
                  Math.min(this.drag.currentX, a + h),
                  l + h
                ))),
            (this.drag.distance > 8 || this.drag.distance < -8) &&
              (i.preventDefault !== s
                ? i.preventDefault()
                : (i.returnValue = !1),
              (this.state.isSwiping = !0)),
            (this.drag.updatedX = this.drag.currentX),
            (this.drag.currentY > 16 || this.drag.currentY < -16) &&
              this.state.isSwiping === !1 &&
              ((this.state.isScrolling = !0),
              (this.drag.updatedX = this.drag.start)),
            this.animate(this.drag.updatedX)));
      }),
      (n.prototype.onDragEnd = function(e) {
        var s, n, o;
        if (this.state.isTouch) {
          if (
            ('mouseup' === e.type && this.$stage.removeClass('owl-grab'),
            this.trigger('dragged'),
            this.drag.targetEl.removeAttribute('draggable'),
            (this.state.isTouch = !1),
            (this.state.isScrolling = !1),
            (this.state.isSwiping = !1),
            0 === this.drag.distance && this.state.inMotion !== !0)
          )
            return (this.state.inMotion = !1), !1;

          (this.drag.endTime = new Date().getTime()),
            (s = this.drag.endTime - this.drag.startTime),
            (n = Math.abs(this.drag.distance)),
            (n > 3 || s > 300) && this.removeClick(this.drag.targetEl),
            (o = this.closest(this.drag.updatedX)),
            this.speed(this.settings.dragEndSpeed || this.settings.smartSpeed),
            this.current(o),
            this.invalidate('position'),
            this.update(),
            this.settings.pullDrag ||
              this.drag.updatedX !== this.coordinates(o) ||
              this.transitionEnd(),
            (this.drag.distance = 0),
            t(i).off('.owl.dragEvents');
        }
      }),
      (n.prototype.removeClick = function(i) {
        (this.drag.targetEl = i),
          t(i).on('click.preventClick', this.e._preventClick),
          e.setTimeout(function() {
            t(i).off('click.preventClick');
          }, 300);
      }),
      (n.prototype.preventClick = function(e) {
        e.preventDefault ? e.preventDefault() : (e.returnValue = !1),
          e.stopPropagation && e.stopPropagation(),
          t(e.target).off('click.preventClick');
      }),
      (n.prototype.getTransformProperty = function() {
        var t, i;
        return (
          (t = e
            .getComputedStyle(this.$stage.get(0), null)
            .getPropertyValue(this.vendorName + 'transform')),
          (t = t.replace(/matrix(3d)?\(|\)/g, '').split(',')),
          (i = 16 === t.length),
          i !== !0 ? t[4] : t[12]
        );
      }),
      (n.prototype.closest = function(e) {
        var i = -1,
          s = 30,
          n = this.width(),
          o = this.coordinates();
        return (
          this.settings.freeDrag ||
            t.each(
              o,
              t.proxy(function(t, r) {
                return (
                  e > r - s && r + s > e
                    ? (i = t)
                    : this.op(e, '<', r) &&
                      this.op(e, '>', o[t + 1] || r - n) &&
                      (i = 'left' === this.state.direction ? t + 1 : t),
                  -1 === i
                );
              }, this)
            ),
          this.settings.loop ||
            (this.op(e, '>', o[this.minimum()])
              ? (i = e = this.minimum())
              : this.op(e, '<', o[this.maximum()]) && (i = e = this.maximum())),
          i
        );
      }),
      (n.prototype.animate = function(e) {
        this.trigger('translate'),
          (this.state.inMotion = this.speed() > 0),
          this.support3d
            ? this.$stage.css({
                transform: 'translate3d(' + e + 'px,0px, 0px)',
                transition: this.speed() / 1e3 + 's'
              })
            : this.state.isTouch
            ? this.$stage.css({
                left: e + 'px'
              })
            : this.$stage.animate(
                {
                  left: e
                },
                this.speed() / 1e3,
                this.settings.fallbackEasing,
                t.proxy(function() {
                  this.state.inMotion && this.transitionEnd();
                }, this)
              );
      }),
      (n.prototype.current = function(t) {
        if (t === s) return this._current;

        if (0 === this._items.length) return s;

        if (((t = this.normalize(t)), this._current !== t)) {
          var e = this.trigger('change', {
            property: {
              name: 'position',
              value: t
            }
          });
          e.data !== s && (t = this.normalize(e.data)),
            (this._current = t),
            this.invalidate('position'),
            this.trigger('changed', {
              property: {
                name: 'position',
                value: this._current
              }
            });
        }
        return this._current;
      }),
      (n.prototype.invalidate = function(t) {
        this._invalidated[t] = !0;
      }),
      (n.prototype.reset = function(t) {
        (t = this.normalize(t)),
          t !== s &&
            ((this._speed = 0),
            (this._current = t),
            this.suppress(['translate', 'translated']),
            this.animate(this.coordinates(t)),
            this.release(['translate', 'translated']));
      }),
      (n.prototype.normalize = function(e, i) {
        var n = i
          ? this._items.length
          : this._items.length + this._clones.length;
        return !t.isNumeric(e) || 1 > n
          ? s
          : (e = this._clones.length
              ? ((e % n) + n) % n
              : Math.max(this.minimum(i), Math.min(this.maximum(i), e)));
      }),
      (n.prototype.relative = function(t) {
        return (
          (t = this.normalize(t)),
          (t -= this._clones.length / 2),
          this.normalize(t, !0)
        );
      }),
      (n.prototype.maximum = function(t) {
        var e,
          i,
          s,
          n = 0,
          o = this.settings;
        if (t) return this._items.length - 1;

        if (!o.loop && o.center) e = this._items.length - 1;
        else if (o.loop || o.center)
          if (o.loop || o.center) e = this._items.length + o.items;
          else {
            if (!o.autoWidth && !o.merge)
              throw 'Can not detect maximum absolute position.';

            for (
              revert = o.rtl ? 1 : -1,
                i = this.$stage.width() - this.$element.width();
              (s = this.coordinates(n)) && !(s * revert >= i);

            )
              e = ++n;
          }
        else e = this._items.length - o.items;

        return e;
      }),
      (n.prototype.minimum = function(t) {
        return t ? 0 : this._clones.length / 2;
      }),
      (n.prototype.items = function(t) {
        return t === s
          ? this._items.slice()
          : ((t = this.normalize(t, !0)), this._items[t]);
      }),
      (n.prototype.mergers = function(t) {
        return t === s
          ? this._mergers.slice()
          : ((t = this.normalize(t, !0)), this._mergers[t]);
      }),
      (n.prototype.clones = function(e) {
        var i = this._clones.length / 2,
          n = i + this._items.length,
          o = function(t) {
            return t % 2 === 0 ? n + t / 2 : i - (t + 1) / 2;
          };
        return e === s
          ? t.map(this._clones, function(t, e) {
              return o(e);
            })
          : t.map(this._clones, function(t, i) {
              return t === e ? o(i) : null;
            });
      }),
      (n.prototype.speed = function(t) {
        return t !== s && (this._speed = t), this._speed;
      }),
      (n.prototype.coordinates = function(e) {
        var i = null;
        return e === s
          ? t.map(
              this._coordinates,
              t.proxy(function(t, e) {
                return this.coordinates(e);
              }, this)
            )
          : (this.settings.center
              ? ((i = this._coordinates[e]),
                (i +=
                  ((this.width() - i + (this._coordinates[e - 1] || 0)) / 2) *
                  (this.settings.rtl ? -1 : 1)))
              : (i = this._coordinates[e - 1] || 0),
            i);
      }),
      (n.prototype.duration = function(t, e, i) {
        return (
          Math.min(Math.max(Math.abs(e - t), 1), 6) *
          Math.abs(i || this.settings.smartSpeed)
        );
      }),
      (n.prototype.to = function(i, s) {
        if (this.settings.loop) {
          var n = i - this.relative(this.current()),
            o = this.current(),
            r = this.current(),
            a = this.current() + n,
            l = 0 > r - a ? !0 : !1,
            h = this._clones.length + this._items.length;
          a < this.settings.items && l === !1
            ? ((o = r + this._items.length), this.reset(o))
            : a >= h - this.settings.items &&
              l === !0 &&
              ((o = r - this._items.length), this.reset(o)),
            e.clearTimeout(this.e._goToLoop),
            (this.e._goToLoop = e.setTimeout(
              t.proxy(function() {
                this.speed(this.duration(this.current(), o + n, s)),
                  this.current(o + n),
                  this.update();
              }, this),
              30
            ));
        } else
          this.speed(this.duration(this.current(), i, s)),
            this.current(i),
            this.update();
      }),
      (n.prototype.next = function(t) {
        (t = t || !1), this.to(this.relative(this.current()) + 1, t);
      }),
      (n.prototype.prev = function(t) {
        (t = t || !1), this.to(this.relative(this.current()) - 1, t);
      }),
      (n.prototype.transitionEnd = function(t) {
        return t !== s &&
          (t.stopPropagation(),
          (t.target || t.srcElement || t.originalTarget) !== this.$stage.get(0))
          ? !1
          : ((this.state.inMotion = !1), void this.trigger('translated'));
      }),
      (n.prototype.viewport = function() {
        var s;
        if (this.options.responsiveBaseElement !== e)
          s = t(this.options.responsiveBaseElement).width();
        else if (e.innerWidth) s = e.innerWidth;
        else {
          if (!i.documentElement || !i.documentElement.clientWidth)
            throw 'Can not detect viewport width.';

          s = i.documentElement.clientWidth;
        }
        return s;
      }),
      (n.prototype.replace = function(e) {
        this.$stage.empty(),
          (this._items = []),
          e && (e = e instanceof jQuery ? e : t(e)),
          this.settings.nestedItemSelector &&
            (e = e.find('.' + this.settings.nestedItemSelector)),
          e
            .filter(function() {
              return 1 === this.nodeType;
            })
            .each(
              t.proxy(function(t, e) {
                (e = this.prepare(e)),
                  this.$stage.append(e),
                  this._items.push(e),
                  this._mergers.push(
                    1 *
                      e
                        .find('[data-merge]')
                        .andSelf('[data-merge]')
                        .attr('data-merge') || 1
                  );
              }, this)
            ),
          this.reset(
            t.isNumeric(this.settings.startPosition)
              ? this.settings.startPosition
              : 0
          ),
          this.invalidate('items');
      }),
      (n.prototype.add = function(t, e) {
        (e = e === s ? this._items.length : this.normalize(e, !0)),
          this.trigger('add', {
            content: t,
            position: e
          }),
          0 === this._items.length || e === this._items.length
            ? (this.$stage.append(t),
              this._items.push(t),
              this._mergers.push(
                1 *
                  t
                    .find('[data-merge]')
                    .andSelf('[data-merge]')
                    .attr('data-merge') || 1
              ))
            : (this._items[e].before(t),
              this._items.splice(e, 0, t),
              this._mergers.splice(
                e,
                0,
                1 *
                  t
                    .find('[data-merge]')
                    .andSelf('[data-merge]')
                    .attr('data-merge') || 1
              )),
          this.invalidate('items'),
          this.trigger('added', {
            content: t,
            position: e
          });
      }),
      (n.prototype.remove = function(t) {
        (t = this.normalize(t, !0)),
          t !== s &&
            (this.trigger('remove', {
              content: this._items[t],
              position: t
            }),
            this._items[t].remove(),
            this._items.splice(t, 1),
            this._mergers.splice(t, 1),
            this.invalidate('items'),
            this.trigger('removed', {
              content: null,
              position: t
            }));
      }),
      (n.prototype.addTriggerableEvents = function() {
        var e = t.proxy(function(e, i) {
          return t.proxy(function(t) {
            t.relatedTarget !== this &&
              (this.suppress([i]),
              e.apply(this, [].slice.call(arguments, 1)),
              this.release([i]));
          }, this);
        }, this);
        t.each(
          {
            next: this.next,
            prev: this.prev,
            to: this.to,
            destroy: this.destroy,
            refresh: this.refresh,
            replace: this.replace,
            add: this.add,
            remove: this.remove
          },
          t.proxy(function(t, i) {
            this.$element.on(t + '.owl.carousel', e(i, t + '.owl.carousel'));
          }, this)
        );
      }),
      (n.prototype.watchVisibility = function() {
        function i(t) {
          return t.offsetWidth > 0 && t.offsetHeight > 0;
        }
        function s() {
          i(this.$element.get(0)) &&
            (this.$element.removeClass('owl-hidden'),
            this.refresh(),
            e.clearInterval(this.e._checkVisibile));
        }
        i(this.$element.get(0)) ||
          (this.$element.addClass('owl-hidden'),
          e.clearInterval(this.e._checkVisibile),
          (this.e._checkVisibile = e.setInterval(t.proxy(s, this), 500)));
      }),
      (n.prototype.preloadAutoWidthImages = function(e) {
        var i, s, n, o;
        (i = 0),
          (s = this),
          e.each(function(r, a) {
            (n = t(a)),
              (o = new Image()),
              (o.onload = function() {
                i++,
                  n.attr('src', o.src),
                  n.css('opacity', 1),
                  i >= e.length &&
                    ((s.state.imagesLoaded = !0), s.initialize());
              }),
              (o.src =
                n.attr('src') ||
                n.attr('data-src') ||
                n.attr('data-src-retina'));
          });
      }),
      (n.prototype.destroy = function() {
        this.$element.hasClass(this.settings.themeClass) &&
          this.$element.removeClass(this.settings.themeClass),
          this.settings.responsive !== !1 && t(e).off('resize.owl.carousel'),
          this.transitionEndVendor &&
            this.off(
              this.$stage.get(0),
              this.transitionEndVendor,
              this.e._transitionEnd
            );
        for (var s in this._plugins) this._plugins[s].destroy();

        (this.settings.mouseDrag || this.settings.touchDrag) &&
          (this.$stage.off('mousedown touchstart touchcancel'),
          t(i).off('.owl.dragEvents'),
          (this.$stage.get(0).onselectstart = function() {}),
          this.$stage.off('dragstart', function() {
            return !1;
          })),
          this.$element.off('.owl'),
          this.$stage.children('.cloned').remove(),
          (this.e = null),
          this.$element.removeData('owlCarousel'),
          this.$stage
            .children()
            .contents()
            .unwrap(),
          this.$stage.children().unwrap(),
          this.$stage.unwrap();
      }),
      (n.prototype.op = function(t, e, i) {
        var s = this.settings.rtl;
        switch (e) {
          case '<':
            return s ? t > i : i > t;
          case '>':
            return s ? i > t : t > i;
          case '>=':
            return s ? i >= t : t >= i;
          case '<=':
            return s ? t >= i : i >= t;
        }
      }),
      (n.prototype.on = function(t, e, i, s) {
        t.addEventListener
          ? t.addEventListener(e, i, s)
          : t.attachEvent && t.attachEvent('on' + e, i);
      }),
      (n.prototype.off = function(t, e, i, s) {
        t.removeEventListener
          ? t.removeEventListener(e, i, s)
          : t.detachEvent && t.detachEvent('on' + e, i);
      }),
      (n.prototype.trigger = function(e, i, s) {
        var n = {
            item: {
              count: this._items.length,
              index: this.current()
            }
          },
          o = t.camelCase(
            t
              .grep(['on', e, s], function(t) {
                return t;
              })
              .join('-')
              .toLowerCase()
          ),
          r = t.Event(
            [e, 'owl', s || 'carousel'].join('.').toLowerCase(),
            t.extend(
              {
                relatedTarget: this
              },
              n,
              i
            )
          );
        return (
          this._supress[e] ||
            (t.each(this._plugins, function(t, e) {
              e.onTrigger && e.onTrigger(r);
            }),
            this.$element.trigger(r),
            this.settings &&
              'function' == typeof this.settings[o] &&
              this.settings[o].apply(this, r)),
          r
        );
      }),
      (n.prototype.suppress = function(e) {
        t.each(
          e,
          t.proxy(function(t, e) {
            this._supress[e] = !0;
          }, this)
        );
      }),
      (n.prototype.release = function(e) {
        t.each(
          e,
          t.proxy(function(t, e) {
            delete this._supress[e];
          }, this)
        );
      }),
      (n.prototype.browserSupport = function() {
        if (((this.support3d = h()), this.support3d)) {
          this.transformVendor = l();
          var t = [
            'transitionend',
            'webkitTransitionEnd',
            'transitionend',
            'oTransitionEnd'
          ];
          (this.transitionEndVendor = t[a()]),
            (this.vendorName = this.transformVendor.replace(/Transform/i, '')),
            (this.vendorName =
              '' !== this.vendorName
                ? '-' + this.vendorName.toLowerCase() + '-'
                : '');
        }
        this.state.orientation = e.orientation;
      }),
      (t.fn.owlCarousel = function(e) {
        return this.each(function() {
          t(this).data('owlCarousel') ||
            t(this).data('owlCarousel', new n(this, e));
        });
      }),
      (t.fn.owlCarousel.Constructor = n);
  })(window.Zepto || window.jQuery, window, document),
  (function(t, e, i, s) {
    var n = function(e) {
      (this._core = e),
        (this._loaded = []),
        (this._handlers = {
          'initialized.owl.carousel change.owl.carousel': t.proxy(function(e) {
            if (
              e.namespace &&
              this._core.settings &&
              this._core.settings.lazyLoad &&
              ((e.property && 'position' == e.property.name) ||
                'initialized' == e.type)
            )
              for (
                var i = this._core.settings,
                  s = (i.center && Math.ceil(i.items / 2)) || i.items,
                  n = (i.center && -1 * s) || 0,
                  o =
                    ((e.property && e.property.value) || this._core.current()) +
                    n,
                  r = this._core.clones().length,
                  a = t.proxy(function(t, e) {
                    this.load(e);
                  }, this);
                n++ < s;

              )
                this.load(r / 2 + this._core.relative(o)),
                  r && t.each(this._core.clones(this._core.relative(o++)), a);
          }, this)
        }),
        (this._core.options = t.extend({}, n.Defaults, this._core.options)),
        this._core.$element.on(this._handlers);
    };
    (n.Defaults = {
      lazyLoad: !1
    }),
      (n.prototype.load = function(i) {
        var s = this._core.$stage.children().eq(i),
          n = s && s.find('.owl-lazy');
        !n ||
          t.inArray(s.get(0), this._loaded) > -1 ||
          (n.each(
            t.proxy(function(i, s) {
              var n,
                o = t(s),
                r =
                  (e.devicePixelRatio > 1 && o.attr('data-src-retina')) ||
                  o.attr('data-src');
              this._core.trigger(
                'load',
                {
                  element: o,
                  url: r
                },
                'lazy'
              ),
                o.is('img')
                  ? o
                      .one(
                        'load.owl.lazy',
                        t.proxy(function() {
                          o.css('opacity', 1),
                            this._core.trigger(
                              'loaded',
                              {
                                element: o,
                                url: r
                              },
                              'lazy'
                            );
                        }, this)
                      )
                      .attr('src', r)
                  : ((n = new Image()),
                    (n.onload = t.proxy(function() {
                      o.css({
                        'background-image': 'url(' + r + ')',
                        opacity: '1'
                      }),
                        this._core.trigger(
                          'loaded',
                          {
                            element: o,
                            url: r
                          },
                          'lazy'
                        );
                    }, this)),
                    (n.src = r));
            }, this)
          ),
          this._loaded.push(s.get(0)));
      }),
      (n.prototype.destroy = function() {
        var t, e;
        for (t in this.handlers) this._core.$element.off(t, this.handlers[t]);

        for (e in Object.getOwnPropertyNames(this))
          'function' != typeof this[e] && (this[e] = null);
      }),
      (t.fn.owlCarousel.Constructor.Plugins.Lazy = n);
  })(window.Zepto || window.jQuery, window, document),
  (function(t, e, i, s) {
    var n = function(e) {
      (this._core = e),
        (this._handlers = {
          'initialized.owl.carousel': t.proxy(function() {
            this._core.settings.autoHeight && this.update();
          }, this),
          'changed.owl.carousel': t.proxy(function(t) {
            this._core.settings.autoHeight &&
              'position' == t.property.name &&
              this.update();
          }, this),
          'loaded.owl.lazy': t.proxy(function(t) {
            this._core.settings.autoHeight &&
              t.element.closest('.' + this._core.settings.itemClass) ===
                this._core.$stage.children().eq(this._core.current()) &&
              this.update();
          }, this)
        }),
        (this._core.options = t.extend({}, n.Defaults, this._core.options)),
        this._core.$element.on(this._handlers);
    };
    (n.Defaults = {
      autoHeight: !1,
      autoHeightClass: 'owl-height'
    }),
      (n.prototype.update = function() {
        this._core.$stage
          .parent()
          .height(
            this._core.$stage
              .children()
              .eq(this._core.current())
              .height()
          )
          .addClass(this._core.settings.autoHeightClass);
      }),
      (n.prototype.destroy = function() {
        var t, e;
        for (t in this._handlers) this._core.$element.off(t, this._handlers[t]);

        for (e in Object.getOwnPropertyNames(this))
          'function' != typeof this[e] && (this[e] = null);
      }),
      (t.fn.owlCarousel.Constructor.Plugins.AutoHeight = n);
  })(window.Zepto || window.jQuery, window, document),
  (function(t, e, i, s) {
    var n = function(e) {
      (this._core = e),
        (this._videos = {}),
        (this._playing = null),
        (this._fullscreen = !1),
        (this._handlers = {
          'resize.owl.carousel': t.proxy(function(t) {
            this._core.settings.video &&
              !this.isInFullScreen() &&
              t.preventDefault();
          }, this),
          'refresh.owl.carousel changed.owl.carousel': t.proxy(function(t) {
            this._playing && this.stop();
          }, this),
          'prepared.owl.carousel': t.proxy(function(e) {
            var i = t(e.content).find('.owl-video');
            i.length && (i.css('display', 'none'), this.fetch(i, t(e.content)));
          }, this)
        }),
        (this._core.options = t.extend({}, n.Defaults, this._core.options)),
        this._core.$element.on(this._handlers),
        this._core.$element.on(
          'click.owl.video',
          '.owl-video-play-icon',
          t.proxy(function(t) {
            this.play(t);
          }, this)
        );
    };
    (n.Defaults = {
      video: !1,
      videoHeight: !1,
      videoWidth: !1
    }),
      (n.prototype.fetch = function(t, e) {
        var i = t.attr('data-vimeo-id') ? 'vimeo' : 'youtube',
          s = t.attr('data-vimeo-id') || t.attr('data-youtube-id'),
          n = t.attr('data-width') || this._core.settings.videoWidth,
          o = t.attr('data-height') || this._core.settings.videoHeight,
          r = t.attr('href');
        if (!r) throw new Error('Missing video URL.');

        if (
          ((s = r.match(
            /(http:|https:|)\/\/(player.|www.)?(vimeo\.com|youtu(be\.com|\.be|be\.googleapis\.com))\/(video\/|embed\/|watch\?v=|v\/)?([A-Za-z0-9._%-]*)(\&\S+)?/
          )),
          s[3].indexOf('youtu') > -1)
        )
          i = 'youtube';
        else {
          if (!(s[3].indexOf('vimeo') > -1))
            throw new Error('Video URL not supported.');

          i = 'vimeo';
        }
        (s = s[6]),
          (this._videos[r] = {
            type: i,
            id: s,
            width: n,
            height: o
          }),
          e.attr('data-video', r),
          this.thumbnail(t, this._videos[r]);
      }),
      (n.prototype.thumbnail = function(e, i) {
        var s,
          n,
          o,
          r =
            i.width && i.height
              ? 'style="width:' + i.width + 'px;height:' + i.height + 'px;"'
              : '',
          a = e.find('img'),
          l = 'src',
          h = '',
          c = this._core.settings,
          p = function(t) {
            (n = '<div class="owl-video-play-icon"></div>'),
              (s = c.lazyLoad
                ? '<div class="owl-video-tn ' +
                  h +
                  '" ' +
                  l +
                  '="' +
                  t +
                  '"></div>'
                : '<div class="owl-video-tn" style="opacity:1;background-image:url(' +
                  t +
                  ')"></div>'),
              e.after(s),
              e.after(n);
          };
        return (
          e.wrap('<div class="owl-video-wrapper"' + r + '></div>'),
          this._core.settings.lazyLoad && ((l = 'data-src'), (h = 'owl-lazy')),
          a.length
            ? (p(a.attr(l)), a.remove(), !1)
            : void ('youtube' === i.type
                ? ((o = 'http://img.youtube.com/vi/' + i.id + '/hqdefault.jpg'),
                  p(o))
                : 'vimeo' === i.type &&
                  t.ajax({
                    type: 'GET',
                    url: 'http://vimeo.com/api/v2/video/' + i.id + '.json',
                    jsonp: 'callback',
                    dataType: 'jsonp',
                    success: function(t) {
                      (o = t[0].thumbnail_large), p(o);
                    }
                  }))
        );
      }),
      (n.prototype.stop = function() {
        this._core.trigger('stop', null, 'video'),
          this._playing.find('.owl-video-frame').remove(),
          this._playing.removeClass('owl-video-playing'),
          (this._playing = null);
      }),
      (n.prototype.play = function(e) {
        this._core.trigger('play', null, 'video'), this._playing && this.stop();
        var i,
          s,
          n = t(e.target || e.srcElement),
          o = n.closest('.' + this._core.settings.itemClass),
          r = this._videos[o.attr('data-video')],
          a = r.width || '100%',
          l = r.height || this._core.$stage.height();
        'youtube' === r.type
          ? (i =
              '<iframe width="' +
              a +
              '" height="' +
              l +
              '" src="http://www.youtube.com/embed/' +
              r.id +
              '?autoplay=1&v=' +
              r.id +
              '" frameborder="0" allowfullscreen></iframe>')
          : 'vimeo' === r.type &&
            (i =
              '<iframe src="http://player.vimeo.com/video/' +
              r.id +
              '?autoplay=1" width="' +
              a +
              '" height="' +
              l +
              '" frameborder="0" webkitallowfullscreen mozallowfullscreen allowfullscreen></iframe>'),
          o.addClass('owl-video-playing'),
          (this._playing = o),
          (s = t(
            '<div style="height:' +
              l +
              'px; width:' +
              a +
              'px" class="owl-video-frame">' +
              i +
              '</div>'
          )),
          n.after(s);
      }),
      (n.prototype.isInFullScreen = function() {
        var s =
          i.fullscreenElement ||
          i.mozFullScreenElement ||
          i.webkitFullscreenElement;
        return (
          s &&
            t(s)
              .parent()
              .hasClass('owl-video-frame') &&
            (this._core.speed(0), (this._fullscreen = !0)),
          s && this._fullscreen && this._playing
            ? !1
            : this._fullscreen
            ? ((this._fullscreen = !1), !1)
            : this._playing && this._core.state.orientation !== e.orientation
            ? ((this._core.state.orientation = e.orientation), !1)
            : !0
        );
      }),
      (n.prototype.destroy = function() {
        var t, e;
        this._core.$element.off('click.owl.video');
        for (t in this._handlers) this._core.$element.off(t, this._handlers[t]);

        for (e in Object.getOwnPropertyNames(this))
          'function' != typeof this[e] && (this[e] = null);
      }),
      (t.fn.owlCarousel.Constructor.Plugins.Video = n);
  })(window.Zepto || window.jQuery, window, document),
  (function(t, e, i, s) {
    var n = function(e) {
      (this.core = e),
        (this.core.options = t.extend({}, n.Defaults, this.core.options)),
        (this.swapping = !0),
        (this.previous = s),
        (this.next = s),
        (this.handlers = {
          'change.owl.carousel': t.proxy(function(t) {
            'position' == t.property.name &&
              ((this.previous = this.core.current()),
              (this.next = t.property.value));
          }, this),
          'drag.owl.carousel dragged.owl.carousel translated.owl.carousel': t.proxy(
            function(t) {
              this.swapping = 'translated' == t.type;
            },
            this
          ),
          'translate.owl.carousel': t.proxy(function(t) {
            this.swapping &&
              (this.core.options.animateOut || this.core.options.animateIn) &&
              this.swap();
          }, this)
        }),
        this.core.$element.on(this.handlers);
    };
    (n.Defaults = {
      animateOut: !1,
      animateIn: !1
    }),
      (n.prototype.swap = function() {
        if (1 === this.core.settings.items && this.core.support3d) {
          this.core.speed(0);
          var e,
            i = t.proxy(this.clear, this),
            s = this.core.$stage.children().eq(this.previous),
            n = this.core.$stage.children().eq(this.next),
            o = this.core.settings.animateIn,
            r = this.core.settings.animateOut;
          this.core.current() !== this.previous &&
            (r &&
              ((e =
                this.core.coordinates(this.previous) -
                this.core.coordinates(this.next)),
              s
                .css({
                  left: e + 'px'
                })
                .addClass('animated owl-animated-out')
                .addClass(r)
                .one(
                  'webkitAnimationEnd mozAnimationEnd MSAnimationEnd oanimationend animationend',
                  i
                )),
            o &&
              n
                .addClass('animated owl-animated-in')
                .addClass(o)
                .one(
                  'webkitAnimationEnd mozAnimationEnd MSAnimationEnd oanimationend animationend',
                  i
                ));
        }
      }),
      (n.prototype.clear = function(e) {
        t(e.target)
          .css({ left: '' })
          .removeClass('animated owl-animated-out owl-animated-in')
          .removeClass(this.core.settings.animateIn)
          .removeClass(this.core.settings.animateOut),
          this.core.transitionEnd();
      }),
      (n.prototype.destroy = function() {
        var t, e;
        for (t in this.handlers) this.core.$element.off(t, this.handlers[t]);

        for (e in Object.getOwnPropertyNames(this))
          'function' != typeof this[e] && (this[e] = null);
      }),
      (t.fn.owlCarousel.Constructor.Plugins.Animate = n);
  })(window.Zepto || window.jQuery, window, document),
  (function(t, e, i, s) {
    var n = function(e) {
      (this.core = e),
        (this.core.options = t.extend({}, n.Defaults, this.core.options)),
        (this.handlers = {
          'translated.owl.carousel refreshed.owl.carousel': t.proxy(function() {
            this.autoplay();
          }, this),
          'play.owl.autoplay': t.proxy(function(t, e, i) {
            this.play(e, i);
          }, this),
          'stop.owl.autoplay': t.proxy(function() {
            this.stop();
          }, this),
          'mouseover.owl.autoplay': t.proxy(function() {
            this.core.settings.autoplayHoverPause && this.pause();
          }, this),
          'mouseleave.owl.autoplay': t.proxy(function() {
            this.core.settings.autoplayHoverPause && this.autoplay();
          }, this)
        }),
        this.core.$element.on(this.handlers);
    };
    (n.Defaults = {
      autoplay: !1,
      autoplayTimeout: 5e3,
      autoplayHoverPause: !1,
      autoplaySpeed: !1
    }),
      (n.prototype.autoplay = function() {
        this.core.settings.autoplay && !this.core.state.videoPlay
          ? (e.clearInterval(this.interval),
            (this.interval = e.setInterval(
              t.proxy(function() {
                this.play();
              }, this),
              this.core.settings.autoplayTimeout
            )))
          : e.clearInterval(this.interval);
      }),
      (n.prototype.play = function(t, s) {
        return i.hidden === !0 ||
          this.core.state.isTouch ||
          this.core.state.isScrolling ||
          this.core.state.isSwiping ||
          this.core.state.inMotion
          ? void 0
          : this.core.settings.autoplay === !1
          ? void e.clearInterval(this.interval)
          : void this.core.next(this.core.settings.autoplaySpeed);
      }),
      (n.prototype.stop = function() {
        e.clearInterval(this.interval);
      }),
      (n.prototype.pause = function() {
        e.clearInterval(this.interval);
      }),
      (n.prototype.destroy = function() {
        var t, i;
        e.clearInterval(this.interval);
        for (t in this.handlers) this.core.$element.off(t, this.handlers[t]);

        for (i in Object.getOwnPropertyNames(this))
          'function' != typeof this[i] && (this[i] = null);
      }),
      (t.fn.owlCarousel.Constructor.Plugins.autoplay = n);
  })(window.Zepto || window.jQuery, window, document),
  (function(t, e, i, s) {
    'use strict';
    var n = function(e) {
      (this._core = e),
        (this._initialized = !1),
        (this._pages = []),
        (this._controls = {}),
        (this._templates = []),
        (this.$element = this._core.$element),
        (this._overrides = {
          next: this._core.next,
          prev: this._core.prev,
          to: this._core.to
        }),
        (this._handlers = {
          'prepared.owl.carousel': t.proxy(function(e) {
            this._core.settings.dotsData &&
              this._templates.push(
                t(e.content)
                  .find('[data-dot]')
                  .andSelf('[data-dot]')
                  .attr('data-dot')
              );
          }, this),
          'add.owl.carousel': t.proxy(function(e) {
            this._core.settings.dotsData &&
              this._templates.splice(
                e.position,
                0,
                t(e.content)
                  .find('[data-dot]')
                  .andSelf('[data-dot]')
                  .attr('data-dot')
              );
          }, this),
          'remove.owl.carousel prepared.owl.carousel': t.proxy(function(t) {
            this._core.settings.dotsData &&
              this._templates.splice(t.position, 1);
          }, this),
          'change.owl.carousel': t.proxy(function(t) {
            if (
              'position' == t.property.name &&
              !this._core.state.revert &&
              !this._core.settings.loop &&
              this._core.settings.navRewind
            ) {
              var e = this._core.current(),
                i = this._core.maximum(),
                s = this._core.minimum();
              t.data =
                t.property.value > i
                  ? e >= i
                    ? s
                    : i
                  : t.property.value < s
                  ? i
                  : t.property.value;
            }
          }, this),
          'changed.owl.carousel': t.proxy(function(t) {
            'position' == t.property.name && this.draw();
          }, this),
          'refreshed.owl.carousel': t.proxy(function() {
            this._initialized || (this.initialize(), (this._initialized = !0)),
              this._core.trigger('refresh', null, 'navigation'),
              this.update(),
              this.draw(),
              this._core.trigger('refreshed', null, 'navigation');
          }, this)
        }),
        (this._core.options = t.extend({}, n.Defaults, this._core.options)),
        this.$element.on(this._handlers);
    };
    (n.Defaults = {
      nav: !1,
      navRewind: !0,
      navText: ['prev', 'next'],
      navSpeed: !1,
      navElement: 'div',
      navContainer: !1,
      navContainerClass: 'owl-nav',
      navClass: ['owl-prev', 'owl-next'],
      slideBy: 1,
      dotClass: 'owl-dot',
      dotsClass: 'owl-dots',
      dots: !0,
      dotsEach: !1,
      dotData: !1,
      dotsSpeed: !1,
      dotsContainer: !1,
      controlsClass: 'owl-controls'
    }),
      (n.prototype.initialize = function() {
        var e,
          i,
          s = this._core.settings;
        s.dotsData ||
          (this._templates = [
            t('<div>')
              .addClass(s.dotClass)
              .append(t('<span>'))
              .prop('outerHTML')
          ]),
          (s.navContainer && s.dotsContainer) ||
            (this._controls.$container = t('<div>')
              .addClass(s.controlsClass)
              .appendTo(this.$element)),
          (this._controls.$indicators = s.dotsContainer
            ? t(s.dotsContainer)
            : t('<div>')
                .hide()
                .addClass(s.dotsClass)
                .appendTo(this._controls.$container)),
          this._controls.$indicators.on(
            'click',
            'div',
            t.proxy(function(e) {
              var i = t(e.target)
                .parent()
                .is(this._controls.$indicators)
                ? t(e.target).index()
                : t(e.target)
                    .parent()
                    .index();
              e.preventDefault(), this.to(i, s.dotsSpeed);
            }, this)
          ),
          (e = s.navContainer
            ? t(s.navContainer)
            : t('<div>')
                .addClass(s.navContainerClass)
                .prependTo(this._controls.$container)),
          (this._controls.$next = t('<' + s.navElement + '>')),
          (this._controls.$previous = this._controls.$next.clone()),
          this._controls.$previous
            .addClass(s.navClass[0])
            .html(s.navText[0])
            .hide()
            .prependTo(e)
            .on(
              'click',
              t.proxy(function(t) {
                this.prev(s.navSpeed);
              }, this)
            ),
          this._controls.$next
            .addClass(s.navClass[1])
            .html(s.navText[1])
            .hide()
            .appendTo(e)
            .on(
              'click',
              t.proxy(function(t) {
                this.next(s.navSpeed);
              }, this)
            );
        for (i in this._overrides) this._core[i] = t.proxy(this[i], this);
      }),
      (n.prototype.destroy = function() {
        var t, e, i, s;
        for (t in this._handlers) this.$element.off(t, this._handlers[t]);

        for (e in this._controls) this._controls[e].remove();

        for (s in this.overides) this._core[s] = this._overrides[s];

        for (i in Object.getOwnPropertyNames(this))
          'function' != typeof this[i] && (this[i] = null);
      }),
      (n.prototype.update = function() {
        var t,
          e,
          i,
          s = this._core.settings,
          n = this._core.clones().length / 2,
          o = n + this._core.items().length,
          r = s.center || s.autoWidth || s.dotData ? 1 : s.dotsEach || s.items;
        if (
          ('page' !== s.slideBy && (s.slideBy = Math.min(s.slideBy, s.items)),
          s.dots || 'page' == s.slideBy)
        )
          for (this._pages = [], t = n, e = 0, i = 0; o > t; t++)
            (e >= r || 0 === e) &&
              (this._pages.push({
                start: t - n,
                end: t - n + r - 1
              }),
              (e = 0),
              ++i),
              (e += this._core.mergers(this._core.relative(t)));
      }),
      (n.prototype.draw = function() {
        var e,
          i,
          s = '',
          n = this._core.settings,
          o = (this._core.$stage.children(),
          this._core.relative(this._core.current()));
        if (
          (!n.nav ||
            n.loop ||
            n.navRewind ||
            (this._controls.$previous.toggleClass('disabled', 0 >= o),
            this._controls.$next.toggleClass(
              'disabled',
              o >= this._core.maximum()
            )),
          this._controls.$previous.toggle(n.nav),
          this._controls.$next.toggle(n.nav),
          n.dots)
        ) {
          if (
            ((e =
              this._pages.length -
              this._controls.$indicators.children().length),
            n.dotData && 0 !== e)
          ) {
            for (i = 0; i < this._controls.$indicators.children().length; i++)
              s += this._templates[this._core.relative(i)];

            this._controls.$indicators.html(s);
          } else
            e > 0
              ? ((s = new Array(e + 1).join(this._templates[0])),
                this._controls.$indicators.append(s))
              : 0 > e &&
                this._controls.$indicators
                  .children()
                  .slice(e)
                  .remove();

          this._controls.$indicators.find('.active').removeClass('active'),
            this._controls.$indicators
              .children()
              .eq(t.inArray(this.current(), this._pages))
              .addClass('active');
        }
        this._controls.$indicators.toggle(n.dots);
      }),
      (n.prototype.onTrigger = function(e) {
        var i = this._core.settings;
        e.page = {
          index: t.inArray(this.current(), this._pages),
          count: this._pages.length,
          size:
            i &&
            (i.center || i.autoWidth || i.dotData ? 1 : i.dotsEach || i.items)
        };
      }),
      (n.prototype.current = function() {
        var e = this._core.relative(this._core.current());
        return t
          .grep(this._pages, function(t) {
            return t.start <= e && t.end >= e;
          })
          .pop();
      }),
      (n.prototype.getPosition = function(e) {
        var i,
          s,
          n = this._core.settings;
        return (
          'page' == n.slideBy
            ? ((i = t.inArray(this.current(), this._pages)),
              (s = this._pages.length),
              e ? ++i : --i,
              (i = this._pages[((i % s) + s) % s].start))
            : ((i = this._core.relative(this._core.current())),
              (s = this._core.items().length),
              e ? (i += n.slideBy) : (i -= n.slideBy)),
          i
        );
      }),
      (n.prototype.next = function(e) {
        t.proxy(this._overrides.to, this._core)(this.getPosition(!0), e);
      }),
      (n.prototype.prev = function(e) {
        t.proxy(this._overrides.to, this._core)(this.getPosition(!1), e);
      }),
      (n.prototype.to = function(e, i, s) {
        var n;
        s
          ? t.proxy(this._overrides.to, this._core)(e, i)
          : ((n = this._pages.length),
            t.proxy(this._overrides.to, this._core)(
              this._pages[((e % n) + n) % n].start,
              i
            ));
      }),
      (t.fn.owlCarousel.Constructor.Plugins.Navigation = n);
  })(window.Zepto || window.jQuery, window, document),
  (function(t, e, i, s) {
    'use strict';
    var n = function(i) {
      (this._core = i),
        (this._hashes = {}),
        (this.$element = this._core.$element),
        (this._handlers = {
          'initialized.owl.carousel': t.proxy(function() {
            'URLHash' == this._core.settings.startPosition &&
              t(e).trigger('hashchange.owl.navigation');
          }, this),
          'prepared.owl.carousel': t.proxy(function(e) {
            var i = t(e.content)
              .find('[data-hash]')
              .andSelf('[data-hash]')
              .attr('data-hash');
            this._hashes[i] = e.content;
          }, this)
        }),
        (this._core.options = t.extend({}, n.Defaults, this._core.options)),
        this.$element.on(this._handlers),
        t(e).on(
          'hashchange.owl.navigation',
          t.proxy(function() {
            var t = e.location.hash.substring(1),
              i = this._core.$stage.children(),
              s = (this._hashes[t] && i.index(this._hashes[t])) || 0;
            return t ? void this._core.to(s, !1, !0) : !1;
          }, this)
        );
    };
    (n.Defaults = {
      URLhashListener: !1
    }),
      (n.prototype.destroy = function() {
        var i, s;
        t(e).off('hashchange.owl.navigation');
        for (i in this._handlers) this._core.$element.off(i, this._handlers[i]);

        for (s in Object.getOwnPropertyNames(this))
          'function' != typeof this[s] && (this[s] = null);
      }),
      (t.fn.owlCarousel.Constructor.Plugins.Hash = n);
  })(window.Zepto || window.jQuery, window, document),
  function(t, e) {
    'object' == typeof exports
      ? (module.exports = e())
      : 'function' == typeof define && define.amd && define('GMaps', [], e),
      (t.GMaps = e());
  };
